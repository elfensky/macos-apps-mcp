---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - macos_apps_mcp/adapters/notes.py
  - tests/test_notes.py
  - macos_apps_mcp/adapters/contacts.py
  - tests/test_contacts.py
  - macos_apps_mcp/adapters/photos.py
  - macos_apps_mcp/adapters/messages.py
  - macos_apps_mcp/adapters/music.py
  - tests/test_music.py
  - macos_apps_mcp/adapters/safari.py
  - tests/test_safari.py
  - macos_apps_mcp/doctor.py
  - tests/test_doctor.py
  - tests/test_native_seam.py
autonomous: true
requirements: [GATE-01, GATE-13]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No module under macos_apps_mcp/adapters/ and not macos_apps_mcp/doctor.py imports run_osascript, body_file or tracked_run by name from runtime — each reaches them as runtime.<name>(...)"
    - "The seam tripwire parametrizes over a sorted list of every adapters/*.py (except __init__.py) plus doctor.py, so a NEW adapter module is checked without anyone adding it (success criterion 1)"
    - "The tripwire asserts it sees ≥ 10 modules including doctor.py and shortcuts.py, so an empty glob fails instead of passing vacuously (GATE-01 empty edge)"
    - "The module list is sorted, so parametrized test ids are stable run to run (GATE-01 ordering edge)"
    - "Card 1 is on origin/develop by one rebase-merged PR whose CI, local verify triple and code-review pass were all clean (D-07)"
  artifacts:
    - path: "tests/test_native_seam.py"
      provides: "static tripwire over every adapter + doctor for by-name seam imports"
      contains: "tracked_run"
    - path: "macos_apps_mcp/doctor.py"
      provides: "qualified runtime.run_osascript for the Automation probe"
      contains: "runtime.run_osascript("
  key_links:
    - from: "tests/test_native_seam.py"
      to: "macos_apps_mcp/adapters/*.py and macos_apps_mcp/doctor.py"
      via: "ast.ImportFrom check against the seam set {run_osascript, body_file, tracked_run}"
      pattern: "_SEAM = frozenset"
  prohibitions:
    - statement: "MUST NOT cherry-pick, rebase or merge a spike commit — card 1 is re-landed by hand on current develop with the spike diff as a recipe"
      status: resolved
      verification: judgment
---

<objective>
Card 1, part B (GATE-01): every remaining adapter and `doctor.py` reach the seam qualified
(`from .. import runtime`, then `runtime.<seam>(...)`), and the static tripwire widens from
`adapters/mail*.py` to every `adapters/*.py` plus `doctor.py`, with `tracked_run` in the seam set.
Then land card 1 as ONE PR (plan 01-02's commits + this plan's) per D-07.

Purpose: together with plan 01-02's runtime lock, a forgotten fake anywhere in the native plane
raises (success criterion 1).
Output: 6 adapters + doctor qualified, their tests patching `runtime.*`, widened tripwire, card 1
merged.

Module inventory (verified on develop): by-name seam imports remain in `contacts.py:16`,
`messages.py:23`, `music.py:16`, `notes.py:30`, `photos.py:13`, `safari.py:11` and
`doctor.py:31` (`shortcuts.py` was fixed in plan 01-02). Names outside the seam set
(`read_via_sqlite`, `mac_region`, `app_process_info`, `request_access_each`, `run_native`) may stay
by-name imports — the spike kept them, and `tests/test_native_seam.py` checks the seam set only.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-PATTERNS.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-02-SUMMARY.md
@AGENTS.md

Recipe: `git diff d9ac75f01eab732509324c0f91087bd587ee6242..spike/arch-review-1-native-seam -- macos_apps_mcp/adapters macos_apps_mcp/doctor.py tests/test_contacts.py tests/test_music.py tests/test_notes.py tests/test_safari.py tests/test_doctor.py tests/test_native_seam.py`.
Apply by hand to the lane's current files (the Sequoia plane changed `doctor.py` after the spike).

Lane: continue in `.worktrees/gate-card-1-native-seam` (branch `refactor/gate-card-1-native-seam`,
pushed by plan 01-02). First `git fetch -q origin`; if `origin/develop` moved (card 9 may have
merged), `git rebase origin/develop` and re-run `uv run pytest` before editing.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: notes, contacts and photos reach the seam qualified (tracer: notes create → runtime.body_file + runtime.run_osascript → conftest lock)</name>
  <files>macos_apps_mcp/adapters/notes.py, tests/test_notes.py, macos_apps_mcp/adapters/contacts.py, tests/test_contacts.py, macos_apps_mcp/adapters/photos.py</files>
  <read_first>
    - macos_apps_mcp/adapters/notes.py (import line 30; every `run_osascript(` and `body_file(` call)
    - tests/test_notes.py (15 `setattr` sites patching the notes module's own seam copies)
    - macos_apps_mcp/adapters/contacts.py (import line 16; calls ~176-183), tests/test_contacts.py
    - macos_apps_mcp/adapters/photos.py (import line 13; call ~64)
    - macos_apps_mcp/adapters/mail.py line ~73 (`from .. import runtime` — the exemplar)
  </read_first>
  <action>
    Per GATE-01 / #176. notes.py: `from .. import runtime` added; the by-name import becomes
    `from ..runtime import read_via_sqlite` (read_via_sqlite is not a seam name); every
    `run_osascript(` → `runtime.run_osascript(` and every `body_file(` → `runtime.body_file(`,
    including the `fallback=lambda: …` and the `_PREVIEW_DELETE`/`_DELETE` paths. contacts.py and
    photos.py: replace `from ..runtime import run_osascript` with `from .. import runtime` and
    qualify every call. Behavior is unchanged — only the lookup moves.

    Tests: every setattr that patched the notes module's own run_osascript/body_file copies now
    patches `runtime` (`monkeypatch.setattr(runtime, "run_osascript", …)` /
    `(runtime, "body_file", …)` with `from macos_apps_mcp import runtime`); same for
    tests/test_contacts.py. Add to tests/test_notes.py `test_create_without_fakes_is_refused`:
    `NotesAdapter().create(NoteData(title="t"))` with no fake raises AssertionError whose message
    contains "body_file" or "run_osascript" — the end-to-end proof that notes' writes now sit
    behind the conftest lock.
  </action>
  <verify>
    <automated>uv run pytest tests/test_notes.py tests/test_contacts.py tests/test_photos.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from \.\.runtime import .*(run_osascript|body_file)" macos_apps_mcp/adapters/notes.py macos_apps_mcp/adapters/contacts.py macos_apps_mcp/adapters/photos.py` prints nothing
    - `grep -c "adapters.notes.run_osascript\|adapters.notes.body_file" tests/test_notes.py` prints 0
    - `uv run pytest tests/test_notes.py -q -k create_without_fakes_is_refused` reports 1 passed
  </acceptance_criteria>
  <done>notes, contacts and photos reach the seam qualified; notes' write path is proven to hit the lock.</done>
</task>

<task type="auto">
  <name>Task 2: messages, music and safari reach the seam qualified</name>
  <files>macos_apps_mcp/adapters/messages.py, macos_apps_mcp/adapters/music.py, tests/test_music.py, macos_apps_mcp/adapters/safari.py, tests/test_safari.py</files>
  <read_first>
    - macos_apps_mcp/adapters/messages.py (import line 23; `_CHATS` call ~292)
    - macos_apps_mcp/adapters/music.py (import line 16; six call sites ~217-265)
    - tests/test_music.py (11 setattr sites), tests/test_safari.py (1 site)
    - macos_apps_mcp/adapters/safari.py (import line 11; calls ~93-98)
  </read_first>
  <action>
    Per GATE-01. messages.py: add `from .. import runtime`; the by-name import becomes
    `from ..runtime import mac_region, read_via_sqlite`; `run_osascript(` → `runtime.run_osascript(`.
    music.py and safari.py: `from ..runtime import run_osascript` → `from .. import runtime`,
    qualify every call. Tests patch `runtime.run_osascript` instead of the module copies.
  </action>
  <verify>
    <automated>uv run pytest tests/test_messages.py tests/test_music.py tests/test_safari.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from \.\.runtime import .*run_osascript" macos_apps_mcp/adapters/messages.py macos_apps_mcp/adapters/music.py macos_apps_mcp/adapters/safari.py` prints nothing
    - `grep -c "adapters.music.run_osascript" tests/test_music.py` prints 0 and `grep -c "adapters.safari.run_osascript" tests/test_safari.py` prints 0
  </acceptance_criteria>
  <done>messages, music and safari reach the seam qualified; their tests patch runtime.</done>
</task>

<task type="auto">
  <name>Task 3: doctor qualified, tripwire widened to every adapter + doctor, and card 1 landed</name>
  <files>macos_apps_mcp/doctor.py, tests/test_doctor.py, tests/test_native_seam.py</files>
  <read_first>
    - macos_apps_mcp/doctor.py (import block lines ~26-32; `_automation_surfaces` call ~167)
    - tests/test_doctor.py (sites patching `doc.run_osascript`)
    - tests/test_native_seam.py (as plan 01-02 left it)
    - AGENTS.md ("Worktrees — one lane, always"), .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md (D-07)
  </read_first>
  <action>
    doctor.py: `from . import deploy` becomes `from . import deploy, runtime`; the runtime import
    becomes `from .runtime import app_process_info, request_access_each, run_native` (drop
    `run_osascript`); `_automation_surfaces` calls `runtime.run_osascript(_PROBE, app, timeout=_PROBE_TIMEOUT)`.
    tests/test_doctor.py: every `monkeypatch.setattr(doc, "run_osascript", …)` →
    `monkeypatch.setattr(runtime, "run_osascript", …)`.

    tests/test_native_seam.py (per GATE-01): `_SEAM = frozenset({"run_osascript", "body_file", "tracked_run"})`;
    `_NATIVE_MODULES` = sorted `(macos_apps_mcp/adapters).glob("*.py")` excluding `__init__.py`,
    plus `macos_apps_mcp/doctor.py`; rename the parametrized test to
    `test_native_module_does_not_import_the_seam_by_name`; the sanity test
    `test_the_tripwire_sees_the_native_modules` asserts `len(_NATIVE_MODULES) >= 10` and that
    `doctor.py` and `shortcuts.py` are in it. Update the module docstring (every adapter + doctor;
    tracked_run joins the seam set; the runtime lock lives in conftest). Keep the plan 01-02 lock
    self-tests.

    Land card 1 (D-07): verify triple in the lane (`uv run pytest`, `uv run ruff check .`,
    `uv run ruff format --check .`); `git push`; `gh pr create --base develop` titled
    "refactor: native seam fails closed for every adapter, doctor and shortcuts (gate card 1, GATE-01)"
    — body: the three-name runtime lock, the widened tripwire, the 36 tests fixed and why, and the
    note that the doctor tests' faked process read lands GATE-11's test side early (Phase 2 keeps
    `_process_name`'s direct `ps`). `gh pr checks --watch --required`. Code-review pass over
    `origin/develop...HEAD` (`code-review` skill, spec = plans 01-02 + 01-04 + GATE-01); fix every
    finding until none is open. If behind `develop`: `git rebase origin/develop`, re-run the
    triple, `git push --force-with-lease`, wait for CI. `gh pr merge --rebase --delete-branch`.
    Clean up the lane (`git worktree unlock` / `remove` / `git branch -D`), `git fetch -q --prune origin`,
    journal the landing (`vault-journal` skill), SUMMARY first line = one-line owner summary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_native_seam.py tests/test_doctor.py -v</automated>
    <fails_when>non-zero exit, or fewer than 10 "test_native_module_does_not_import_the_seam_by_name[" lines in the output</fails_when>
    <automated>set -o pipefail; git fetch -q origin && git show origin/develop:tests/test_native_seam.py | grep -c "tracked_run"</automated>
    <fails_when>non-zero exit, or the count prints 0 (card 1 not on develop)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "from \.runtime import .*run_osascript" macos_apps_mcp/doctor.py` prints nothing; `grep -c "runtime.run_osascript(" macos_apps_mcp/doctor.py` prints a number ≥ 1
    - `uv run pytest tests/test_native_seam.py -q` passes with one parametrized case per adapter module plus doctor.py (≥ 10)
    - Adding a scratch module `macos_apps_mcp/adapters/zz_probe.py` containing `from ..runtime import tracked_run` makes `uv run pytest tests/test_native_seam.py -q` fail (checked, then the scratch file deleted — recorded in the SUMMARY)
    - The card-1 PR is `MERGED` via rebase; CI green; review clean
  </acceptance_criteria>
  <done>Every adapter and doctor reach the seam qualified, the tripwire covers the whole native plane, and card 1 is on develop.</done>
</task>

</tasks>
<!-- plan-criteria-allow: R4 - the piped git verify starts with set -o pipefail, so the pipeline reports git's own exit status -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit test → native seam | same boundary as plan 01-02; this plan closes the by-name copies that escaped the lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-10 | Tampering | a by-name seam copy in an adapter escapes the conftest lock and reaches a real app | high | mitigate | all seven modules qualified; the static tripwire over adapters/*.py + doctor.py fails on any by-name seam import, including in a new module |
| T-1-11 | Tampering | osascript argv injection through qualified calls | low | accept | argument passing is unchanged (runtime's `--` separator guard untouched); canon injection review belongs to /gsd-secure-phase |
</threat_model>

<verification>
- `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .` pass on the merged head.
- The tripwire fails for a by-name seam import in a new adapter module.
</verification>

<success_criteria>
Success criterion 1 holds: a forgotten fake raises for every adapter, doctor and shortcuts'
tracked_run; the three names are locked once in conftest; the tripwire catches a new unlocked module.
</success_criteria>

## Artifacts this phase produces

- `tests/test_native_seam.py::_NATIVE_MODULES`, `::test_native_module_does_not_import_the_seam_by_name`, `::test_the_tripwire_sees_the_native_modules` (renamed/widened from the mail-only versions)
- `tests/test_notes.py::test_create_without_fakes_is_refused`
- Card-1 PR on `develop` (branch `refactor/gate-card-1-native-seam`, deleted after merge)

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-04-SUMMARY.md` when done.
</output>
