---
context: phase
phase: 01-gate-land-the-spiked-architecture-cuts
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-25T12:10:09.458Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Early requirement tick | The 01-01 executor marked GATE-13 complete, but ten plans carry GATE-13. The tick was reverted. | advisory | Every dispatch prompt forbids edits to REQUIREMENTS/STATE/ROADMAP. The orchestrator ticks a requirement only at its last plan (map below). |
| Plain `git pull --ff-only` in the main checkout | Local `develop` carries unpushed `.planning` commits, so ff-only fails after every PR merge. | advisory | Sync with `git pull --rebase --autostash`. |

<current_state>
Phase 1 is paused in the middle of wave 3 of 11. 3 of 14 plans are complete. The computer restart
stopped the two wave-3 executors. Both lanes are clean: all work is committed, nothing is lost.

- **01-04 (card 1 part B, GATE-01):** Tasks 1–2 and the Task 3 code are committed. The lane
  `.worktrees/gate-card-1-native-seam` (locked, tip `26b13ed`) is rebased on `origin/develop`
  and pushed. PR #214 is open, and its CI `check` passes. Still to do: re-run the local triple,
  do the code-review pass (Standards + Spec), rebase-merge #214, remove the lane, write the
  journal bullet, write `01-04-SUMMARY.md`.
- **01-05 (card 4, GATE-09):** Tasks 1–2 are committed in the lane
  `.worktrees/gate-card-4-recoverable-preflight` (locked, `2649218`, `30f6ded`, NOT pushed). The
  v0.11.0 baseline `tests/mail_recover_dry_run_baseline.json` is captured. Still to do: Task 3
  (push, open the PR, CI green, review clean, and do NOT merge), then `01-05-SUMMARY.md`.
</current_state>

<completed_work>
- 01-01: v0.11.0 is released and installed. PR #211 (bump) and PR #212 (cut to `main`) are merged. `main` is `f52e9d5` with tag `v0.11.0`. The GitHub release carries the notarized zip. The daemon probe shows version 0.11.0, build `f52e9d5`.
- 01-02: card 1 part A. The conftest lock covers `run_osascript`, `tracked_run` and `body_file` at runtime. The commits sit on the card 1 branch and land with PR #214.
- 01-03: card 9 is landed (PR #213, `0cfc0f0` and `36624be`). The tripwire covers 52 call sites. `_DEDUPE` is 900 s.
- Tracking commits on local `develop` (not pushed): `e3c3b8f`, `407ed42`.
</completed_work>

<remaining_work>
- Wave 3: finish 01-04 and 01-05 (see current_state).
- Wave 4: 01-06 (card 7, runtime split) and 01-07 (card 3, envelope fixture). Their `files_modified` lists do not overlap, so they can run in parallel.
- Wave 5: 01-08 is an OWNER DEVICE STOP. It covers card 7's `pytest -m integration -k "request_access or create_event or create_reminder"` and card 4's scratch-mailbox check with the watchdog. Card 4 merges after this stop.
- Waves 6–11: 01-09 card 5, 01-10 dev-build check (owner stop), 01-11/12/13 card 2 A/B/C, 01-14 final dev-build check (owner stop) and spike-branch cleanup (D-09).
- Then the phase gates: code review, verifier, update_roadmap.
</remaining_work>

<decisions_made>
- Isolation is `none`: dispatch is sequential from the main checkout, and every plan makes its own `.worktrees/<slug>` lane per the plan. Harness worktrees would nest the lanes and delete `.worktrees/.daemon_probe.py`, which 01-10 and 01-14 reuse.
- Same-wave plans run in parallel as background executors. Each executor writes its SUMMARY uncommitted and never touches STATE/ROADMAP/REQUIREMENTS. The orchestrator runs the post-merge gate (`pytest`, `ruff`), `roadmap.update-plan-progress` and `state.advance-plan`, and commits after each wave.
- Executors read the protocol files (`execute-plan.md`, `summary.md`, `checkpoints.md`, `executor-examples.md`) with the Read tool. The orchestrator does not inline them.
- Executors cannot start sub-agents, so they run the `code-review` skill's Standards and Spec axes themselves, one after the other (D-07).
- Last plan per requirement (tick it only then): GATE-01=01-04, GATE-08=01-07, GATE-02=01-08, GATE-09=01-08, GATE-10=01-09, GATE-06=01-11, GATE-03=01-12, GATE-04=01-12, GATE-05=01-13, GATE-13=01-14.
- The owner answered "cut-now" at 01-01 Task 2.
</decisions_made>

<blockers>
None. The next owner stop is 01-08 (wave 5).
</blockers>

## Required Reading (in order)
1. This file and `.planning/HANDOFF.json`
2. `01-CONTEXT.md`: D-05 to D-09 set the landing flow and the owner stops
3. `01-04-PLAN.md` Task 3 and `01-05-PLAN.md` Task 3: where each executor resumes
4. `docs/mail-applescript-facts.md` before any card 4 work

## Infrastructure State
- Daemon: v0.11.0, build `f52e9d5`, installed and running. Every MCP client must reconnect (`/mcp`).
- Lanes (all under `.worktrees/`, which is git-ignored): `gate-card-1-native-seam` (locked), `gate-card-4-recoverable-preflight` (locked), `baseline-v0.11.0` (detached at the tag, keep for 01-08), `.daemon_probe.py` (keep for 01-10/01-14).
- Main checkout: `develop` is 2 commits ahead of `origin/develop` (`.planning` only), behind 0.
- Leave these alone, they belong to another session: `.planning/state.json`, `.planning/milestone.lock`, `.gsd/`, `.planning/phases/02.1-*`. `.planning/config.json` has the execute-phase `_auto_chain_active=false` edit.
- The old `spike/arch-review-*` worktrees under `.claude/worktrees/` stay until 01-14 (D-09).

<next_action>
Start with: `/gsd-execute-phase 1`. It skips 01-01 to 01-03 and dispatches wave 3. In each executor
prompt, say RESUME. 01-04 continues from PR #214 (CI passing) at the verify and review step.
01-05 starts at Task 3 in its existing lane. Neither executor creates a new lane.
</next_action>
