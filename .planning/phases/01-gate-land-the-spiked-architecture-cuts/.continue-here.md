---
context: phase
phase: 01-gate-land-the-spiked-architecture-cuts
task: 0
total_tasks: 3
status: in_progress
last_updated: 2026-09-25T19:43:39.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Early requirement tick | The 01-01 executor marked GATE-13 complete, but ten plans carry GATE-13. The tick was reverted. | advisory | Every dispatch prompt forbids edits to REQUIREMENTS/STATE/ROADMAP. The orchestrator ticks a requirement only at its last plan (map below). |
| Plain `git pull --ff-only` in the main checkout | Local `develop` carries unpushed `.planning` commits, so ff-only fails after every PR merge. | advisory | Sync with `git pull --rebase --autostash`. |
| `requirements mark-complete` reformats REQUIREMENTS.md | The gsd-tools verb adds and removes blank lines across the whole file. | advisory | Tick with a two-line `sed` on the checkbox and the traceability row instead. |
| `state.begin-phase` on a resumed phase | It resets "Plan: N of 14" to 1. | advisory | Restore the line after the call, or skip the call on resume. |

<current_state>
Phase 1 stops at the start of wave 8 of 11. 10 of 14 plans are complete.
The installed daemon is a signed dev build of develop `bd6fe53` (plan 01-10): version 0.11.0,
outbound gated to a dry-run preview, mail_index ok. A byte-identical backup of the v0.11.0 app is in
the 2026-09-25 session scratchpad.

- Landed on develop: card 1 (#214), card 9 (#213), card 7 (#216), card 3 (#217), card 4 (#215), card 5 (#218).
- Ticked: GATE-01, GATE-02, GATE-08, GATE-09, GATE-10.
- Next: 01-11 (card 2 part A), autonomous. It needs a normal executor dispatch, so run it from a NEW chat.
</current_state>

<completed_work>
- 01-01: v0.11.0 released and installed (daemon version 0.11.0, build `f52e9d5`).
- 01-02 + 01-04: card 1 — native seam locked at runtime for all three names; tripwire covers every adapter plus `doctor.py`.
- 01-03: card 9 — script-timeout tripwire; `_DEDUPE` backstop 900 s.
- 01-05: card 4 PR #215 — `recoverable()` owns the dry-run read; review fix `9cc4fe5`.
- 01-06: card 7 — EventKit cluster in `eventkit.py`; `runtime.py` is an 11-name native door. Device proof queued for 01-08.
- 01-07: card 3 — shared `tests/envelope.py` fixture in native and Sequoia shape; `HEADER_FINGERPRINT` covers every read column.
</completed_work>

<remaining_work>
- Waves 8–10: 01-11/12/13 card 2 A/B/C (autonomous, one lane each, rebase-merged PRs).
- Wave 11: 01-14 final dev-build check (owner stop) and spike-branch cleanup (D-09).
- Then the phase gates: code review, verifier, update_roadmap.
- Pending todos in STATE.md: stale `runtime.from_nsdate` docstring in `adapters/messages.py`; installed bundle seal broken by runtime `__pycache__` writes.
</remaining_work>

<decisions_made>
- The isolation sentinel expires: re-run the force command before EVERY dispatch. Isolation resolves to `none` while local `develop` carries unpushed `.planning` commits (fork-base check degrades). Force it with `gsd_run query dispatch-isolation --raw --phase 01 --force-isolation none` before dispatch, or the Agent isolation guard hook refuses a dispatch without `isolation="worktree"`.
- Same-wave plans run in parallel as background executors, each in its own `.worktrees/<slug>` lane. The `<project_root_pin>` guard is bound to the plan's LANE path, not the main checkout: the stock pin fails inside a linked worktree.
- Executors write the SUMMARY uncommitted and never touch STATE/ROADMAP/REQUIREMENTS or the main checkout's git state beyond `git fetch` and `git worktree` commands. The orchestrator syncs, runs the post-merge triple, updates tracking and commits after each wave.
- Executors cannot start sub-agents, so they run the `code-review` skill's Standards and Spec axes themselves, one after the other (D-07).
- Last plan per requirement (tick it only then): GATE-06=01-11, GATE-03=01-12, GATE-04=01-12, GATE-05=01-13, GATE-13=01-14. Done: GATE-01 (01-04), GATE-08 (01-07), GATE-02 + GATE-09 (01-08), GATE-10 (01-09).
- Do NOT dispatch 01-10 or 01-14 with `isolation="worktree"`: a harness hook confines a worktree agent's git commands and file writes to its own worktree, so it cannot add the build worktree in the main repo or write its SUMMARY there (tried 2026-09-25, stopped before any build; daemon untouched).
- A session hook (Toyota lock) blocks `gsd-tools.cjs` once a session has touched Toyota material. Such a session cannot renew the isolation sentinel; start a fresh session outside the vault for GSD tooling.
</decisions_made>

<blockers>
None for 01-11. The 2026-09-25 conversation is Toyota-locked: it cannot run `gsd-tools.cjs`, so it cannot renew the isolation sentinel or dispatch executors. Resume in a new chat, not a resumed one.
</blockers>

## Required Reading (in order)
1. This file and `.planning/HANDOFF.json`
2. `01-11-PLAN.md`
3. `01-09-SUMMARY.md` (card 5: the provisional ledger in `tiers.py` that card 2 replaces) and `01-10-SUMMARY.md`

## Infrastructure State
- Daemon: dev build `bd6fe53 2026-09-25T19:39:50Z` of 0.11.0, installed by 01-10. Clients must reconnect with `/mcp`.
- `.worktrees/` holds only `.daemon_probe.py` (keep for 01-14). All card lanes and `baseline-v0.11.0` are removed.
- Main checkout: `develop` carries unpushed `.planning` commits only; behind 0.
- Leave alone (another session): `.planning/state.json`, `.planning/milestone.lock`, `.gsd/`, `.planning/phases/02.1-*`. `.planning/config.json` has the execute-phase `_auto_chain_active=false` edit.
- The `spike/arch-review-*` worktrees under `.claude/worktrees/` stay until 01-14 (D-09).

<next_action>
In a NEW Claude Code chat: `/gsd-execute-phase 1`. It skips 01-01 to 01-10 (SUMMARY present) and dispatches wave 8 (01-11). Renew the isolation sentinel before each dispatch.
</next_action>
