---
context: phase
phase: 01-gate-land-the-spiked-architecture-cuts
task: 1
total_tasks: 3
status: checkpoint
last_updated: 2026-09-25T13:50:00.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Early requirement tick | The 01-01 executor marked GATE-13 complete, but ten plans carry GATE-13. The tick was reverted. | advisory | Every dispatch prompt forbids edits to REQUIREMENTS/STATE/ROADMAP. The orchestrator ticks a requirement only at its last plan (map below). |
| Plain `git pull --ff-only` in the main checkout | Local `develop` carries unpushed `.planning` commits, so ff-only fails after every PR merge. | advisory | Sync with `git pull --rebase --autostash`. |
| `requirements mark-complete` reformats REQUIREMENTS.md | The gsd-tools verb adds and removes blank lines across the whole file. | advisory | Tick with a two-line `sed` on the checkbox and the traceability row instead. |
| `state.begin-phase` on a resumed phase | It resets "Plan: N of 14" to 1. | advisory | Restore the line after the call, or skip the call on resume. |

<current_state>
Phase 1 stops at the start of wave 5 of 11. 7 of 14 plans are complete. Waves 3 and 4 ran in
this session. The next plan, 01-08, opens with a blocking owner decision (Task 1, `blocking-human`).

- Card 1 landed (PR #214). Card 7 landed (PR #216). Card 3 landed (PR #217). Card 9 landed earlier (PR #213).
- Card 4 is PR #215: open, CI green, review clean, mergeable, NOT merged. It merges only in 01-08 Task 3.
- GATE-01 and GATE-08 are ticked. GATE-02 and GATE-09 tick at 01-08.
</current_state>

<completed_work>
- 01-01: v0.11.0 released and installed (daemon version 0.11.0, build `f52e9d5`).
- 01-02 + 01-04: card 1 — native seam locked at runtime for all three names; tripwire covers every adapter plus `doctor.py`.
- 01-03: card 9 — script-timeout tripwire; `_DEDUPE` backstop 900 s.
- 01-05: card 4 PR #215 — `recoverable()` owns the dry-run read; review fix `9cc4fe5`.
- 01-06: card 7 — EventKit cluster in `eventkit.py`; `runtime.py` is an 11-name native door. Device proof queued for 01-08.
- 01-07: card 3 — shared `tests/envelope.py` fixture in native and Sequoia shape; `HEADER_FINGERPRINT` covers every read column.
</completed_work>

<remaining_work>
- Wave 5: 01-08 — OWNER DEVICE SESSION. Task 1 asks "run-now" or "later". Task 2: card 7 EventKit integration selector on develop. Task 3: card 4 scratch-mailbox round trip with the watchdog, device dry-run identity diff, then merge PR #215.
- Waves 6–11: 01-09 card 5, 01-10 dev-build check (owner stop), 01-11/12/13 card 2 A/B/C, 01-14 final dev-build check (owner stop) and spike-branch cleanup (D-09).
- Then the phase gates: code review, verifier, update_roadmap.
- Pending todo: stale `runtime.from_nsdate` reference in `adapters/messages.py` docstring (see STATE.md).
</remaining_work>

<decisions_made>
- Isolation resolves to `none` while local `develop` carries unpushed `.planning` commits (fork-base check degrades). Force it with `gsd_run query dispatch-isolation --raw --phase 01 --force-isolation none` before dispatch, or the Agent isolation guard hook refuses a dispatch without `isolation="worktree"`.
- Same-wave plans run in parallel as background executors, each in its own `.worktrees/<slug>` lane. The `<project_root_pin>` guard is bound to the plan's LANE path, not the main checkout: the stock pin fails inside a linked worktree.
- Executors write the SUMMARY uncommitted and never touch STATE/ROADMAP/REQUIREMENTS or the main checkout's git state beyond `git fetch` and `git worktree` commands. The orchestrator syncs, runs the post-merge triple, updates tracking and commits after each wave.
- Executors cannot start sub-agents, so they run the `code-review` skill's Standards and Spec axes themselves, one after the other (D-07).
- Last plan per requirement (tick it only then): GATE-02=01-08, GATE-09=01-08, GATE-10=01-09, GATE-06=01-11, GATE-03=01-12, GATE-04=01-12, GATE-05=01-13, GATE-13=01-14. Done: GATE-01 (01-04), GATE-08 (01-07).
</decisions_made>

<blockers>
01-08 Task 1 needs the owner's answer: "run-now" or "later". Preconditions checked at 2026-09-25 15:03: the watchdog LaunchAgent `ren.lav.mail-watchdog` is loaded and logging; Mail is running and idle.
</blockers>

## Required Reading (in order)
1. This file and `.planning/HANDOFF.json`
2. `01-08-PLAN.md`
3. `01-05-SUMMARY.md` (PR #215) and `01-06-SUMMARY.md` (card 7 merge sha `8627aa8`)
4. `docs/mail-applescript-facts.md` before any card 4 work

## Infrastructure State
- Daemon: v0.11.0, build `f52e9d5`. Develop has cards 1, 3, 7 and 9 since; the daemon does not, until a rebuild (01-10).
- Lanes under `.worktrees/`: `gate-card-4-recoverable-preflight` (locked, `9cc4fe5`, pushed), `baseline-v0.11.0` (detached at the tag), `.daemon_probe.py`. Keep all three for 01-08 / 01-10 / 01-14.
- Main checkout: `develop` is 5 `.planning` commits ahead of `origin/develop`, behind 0.
- Leave alone (another session): `.planning/state.json`, `.planning/milestone.lock`, `.gsd/`, `.planning/phases/02.1-*`. `.planning/config.json` has the execute-phase `_auto_chain_active=false` edit.
- The `spike/arch-review-*` worktrees under `.claude/worktrees/` stay until 01-14 (D-09).

<next_action>
Owner answers 01-08 Task 1. On "run-now": `/gsd-execute-phase 1` dispatches 01-08 with the go recorded; the owner stays at the Mac for TCC prompts and to watch Mail.
</next_action>
