---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/conftest.py
  - macos_apps_mcp/adapters/shortcuts.py
  - tests/test_shortcuts.py
  - tests/test_doctor.py
  - tests/test_doctor_deploy.py
  - tests/test_runtime.py
  - tests/test_mail.py
  - tests/test_mail_drafts.py
  - tests/test_native_seam.py
autonomous: true
requirements: [GATE-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A unit test that calls runtime.tracked_run, runtime.body_file or runtime.run_osascript without faking it raises AssertionError naming the seam — it never spawns a process or writes a tempfile for a real app"
    - "ShortcutsAdapter reaches tracked_run qualified (runtime.tracked_run); a test that forgets the fake raises instead of running the shortcuts CLI"
    - "doctor's unit tests run no live pgrep/ps: their process read goes through a faked runtime.tracked_run"
    - "A test's own monkeypatch of a seam name overrides the autouse lock (the lock applies first), so a faked seam is used and an unfaked one raises (GATE-01 adjacency edge)"
    - "uv run pytest, uv run ruff check . and uv run ruff format --check . pass on the lane branch"
  artifacts:
    - path: "tests/conftest.py"
      provides: "autouse _no_real_osascript fixture that refuses run_osascript, body_file and tracked_run"
      contains: "tracked_run"
    - path: "macos_apps_mcp/adapters/shortcuts.py"
      provides: "qualified seam access"
      contains: "runtime.tracked_run("
    - path: "tests/test_native_seam.py"
      provides: "runtime-lock self-tests for all three seam names"
      contains: "body_file"
  key_links:
    - from: "tests/conftest.py::_no_real_osascript"
      to: "macos_apps_mcp/runtime.py"
      via: "monkeypatch.setattr(runtime, <seam>, refuse) for each of run_osascript, body_file, tracked_run"
      pattern: "setattr\\(runtime"
    - from: "macos_apps_mcp/adapters/shortcuts.py"
      to: "macos_apps_mcp/runtime.py::tracked_run"
      via: "from .. import runtime; runtime.tracked_run(...)"
      pattern: "runtime\\.tracked_run\\("
  prohibitions:
    - statement: "MUST NOT exempt a whole test module or the suite from the native-seam lock; only a seam's own self-test binds the real function, per test"
      status: resolved
      verification: judgment
---

<objective>
Card 1, part A (GATE-01): make the native seam fail closed at RUNTIME for all three seam names
— `run_osascript` (already locked), `tracked_run` and `body_file` — once, in `tests/conftest.py`.
This goes beyond the spike: spike 1's conftest diff changed only the docstring (RESEARCH Pitfall 1).

Purpose: a unit test that forgets a fake can never reach a real app or process.
Output: extended conftest lock, `shortcuts.py` qualified, doctor tests off live `pgrep`/`ps`,
seam self-tests bound to the real functions, lock self-tests. No PR yet — plan 01-04 finishes
card 1 on the same branch and opens the one card-1 PR (D-07).

Measured before planning (scratch pytest plugin locking both names on current develop): 36 tests
fail — 18 doctor tests (`test_doctor.py`, `test_doctor_deploy.py`: live `pgrep` through
`runtime.app_process_info`), 13 Mail tests that use a real `body_file` tempfile
(`test_mail.py` 9, `test_mail_drafts.py` 4), 2 `body_file` self-tests in `test_runtime.py`, and 3
`tracked_run` tests in `test_shortcuts.py`. Every one is fixed here.

Cross-phase note: faking the doctor tests' process read lands the test-side of GATE-11 (Phase 2)
early, because the lock cannot land green without it. `doctor._process_name`'s direct
`subprocess.run(["ps", ...])` stays for Phase 2.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-RESEARCH.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-PATTERNS.md
@AGENTS.md

Recipe (never applied literally — CONTEXT "Re-land method"):
`git diff d9ac75f01eab732509324c0f91087bd587ee6242..spike/arch-review-1-native-seam -- macos_apps_mcp/adapters/shortcuts.py tests/test_shortcuts.py tests/conftest.py`.
Redo by hand on current code; never cherry-pick or rebase a spike commit.

Lane (AGENTS.md, PR #209): `.worktrees/gate-card-1-native-seam`, branch
`refactor/gate-card-1-native-seam`, cut from `origin/develop` AFTER plan 01-01 merged the 0.11.0
bump. Plan 01-04 continues on this same lane and branch. Do not edit `pyproject.toml` (the
`version` workflow would demand a bump).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tracked_run locked end-to-end through the shortcuts adapter (tracer)</name>
  <files>tests/conftest.py, macos_apps_mcp/adapters/shortcuts.py, tests/test_shortcuts.py, tests/test_doctor.py, tests/test_doctor_deploy.py</files>
  <read_first>
    - tests/conftest.py (fixture `_no_real_osascript`, lines ~143-162)
    - macos_apps_mcp/adapters/shortcuts.py (import line 21; call sites ~49 and ~175)
    - tests/test_shortcuts.py (every setattr on the shortcuts module's tracked_run copy, the identity test ~line 366, and the two tracked_run self-tests ~373-420)
    - tests/test_doctor.py and tests/test_doctor_deploy.py (tests that call `doc.diagnose()` / `doc._automation_surfaces()`)
    - macos_apps_mcp/runtime.py (`app_process_info`, `tracked_run`: app_process_info catches OSError/SubprocessError/ValueError/IndexError only, so the lock's AssertionError propagates)
    - tests/test_runtime.py (module-level import block, lines ~32-45: the existing convention of binding the real `run_osascript` at import time)
  </read_first>
  <action>
    Create the lane: `git fetch -q --prune origin`, then
    `git worktree add --lock --reason "$(hostname -s)" .worktrees/gate-card-1-native-seam -b refactor/gate-card-1-native-seam origin/develop`,
    then `uv sync` in it. Work only inside the lane.

    conftest: extend the existing autouse fixture `_no_real_osascript` (keep its name) so it
    refuses `runtime.tracked_run` as well as `runtime.run_osascript`. Build one refusal per seam
    name whose AssertionError message names that seam and the fake to use, e.g.
    "a unit test reached tracked_run — fake it with monkeypatch.setattr(runtime, 'tracked_run', ...)".
    Integration tests stay exempt (`"integration" in request.keywords`). Update the docstring:
    the lock covers the named seams; a test that monkeypatches `runtime.<seam>` itself overrides
    the lock; a seam's own self-tests use the real function bound at test-module import time
    (the `tests/test_runtime.py` convention for `run_osascript`).

    shortcuts.py: replace `from ..runtime import tracked_run` with `from .. import runtime` and
    call `runtime.tracked_run(...)` at both call sites (per GATE-01; #176 qualified-seam rule).

    test_shortcuts.py: every setattr that patched the shortcuts module's own tracked_run copy now
    patches `macos_apps_mcp.runtime.tracked_run` instead. The identity test
    asserts `shortcuts.runtime is runtime` and `not hasattr(shortcuts, "tracked_run")`. The two
    `tracked_run` self-tests (`test_tracked_run_registers_child_while_running`,
    `test_tracked_run_kills_reaps_and_reraises_on_timeout`) call the real function bound at module
    import (`from macos_apps_mcp.runtime import tracked_run as _real_tracked_run` in the
    module-level import block), still with `runtime.subprocess.Popen` faked. Add
    `test_unfaked_tracked_run_is_refused`: `ShortcutsAdapter().get_pointers("x")` with no fake
    raises AssertionError matching "tracked_run" — the end-to-end tracer proof
    (adapter → runtime seam → conftest lock).

    test_doctor.py and test_doctor_deploy.py: add one module-level autouse fixture per file
    (e.g. `_no_live_process_probe`) that sets `runtime.tracked_run` to a fake returning
    `subprocess.CompletedProcess(cmd, 1, "", "")` — "pgrep found nothing" — so
    `runtime.app_process_info` returns None and no live `pgrep`/`ps` runs. Tests that already
    patch `doc.app_process_info` keep working unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_shortcuts.py tests/test_doctor.py tests/test_doctor_deploy.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" in the summary line</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any "FAILED" line in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "from ..runtime import tracked_run" macos_apps_mcp/adapters/shortcuts.py` prints nothing; `grep -c "runtime.tracked_run(" macos_apps_mcp/adapters/shortcuts.py` prints a number ≥ 1
    - `grep -c "adapters.shortcuts.tracked_run" tests/test_shortcuts.py` prints 0
    - `uv run pytest tests/test_shortcuts.py -q -k unfaked_tracked_run` reports 1 passed
    - `uv run pytest tests/test_doctor.py tests/test_doctor_deploy.py -q` passes with the tracked_run lock active (so no live pgrep ran — the lock would have raised)
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>tracked_run is refused by default in unit tests; shortcuts reaches it qualified; the doctor tests are hermetic; the suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: body_file locked; seam self-tests and Mail tempfile tests made explicit; lock self-tests</name>
  <files>tests/conftest.py, tests/test_runtime.py, tests/test_mail.py, tests/test_mail_drafts.py, tests/test_native_seam.py</files>
  <read_first>
    - tests/conftest.py (fixture as Task 1 left it)
    - tests/test_runtime.py (body_file tests ~547-570 import `body_file` inside the function — that import sees the locked attribute)
    - tests/test_mail.py (`_patch_body_file` helper ~line 42; the reply/send tests that use a real tempfile, e.g. test_reply_cleans_up_tempfile, test_send_passes_addresses_via_argv_us_joined)
    - tests/test_mail_drafts.py (test_create_draft_passes_body_via_tempfile, test_create_draft_cleans_up_tempfile, test_create_draft_returns_locator_dict, test_create_draft_propagates_error_and_cleans_tempfile)
    - tests/test_native_seam.py (current file, 43 lines)
  </read_first>
  <behavior>
    - Test: for each name in ("run_osascript", "body_file", "tracked_run"), calling `getattr(runtime, name)` with any args inside a unit test raises AssertionError whose message contains that name
    - Test: a test that monkeypatches `runtime.body_file` to a fake gets the fake (the lock is overridden, not stacked)
    - Test: the 13 Mail tests measured failing under the lock pass again, each either with a fake body_file or with the real one restored explicitly
  </behavior>
  <action>
    conftest: add `body_file` to the refused seam names in `_no_real_osascript` (same refusal shape
    as Task 1).

    test_runtime.py: move the in-function `from macos_apps_mcp.runtime import body_file` imports of
    the two body_file self-tests into the module-level import block, so they bind the real
    function at collection time (same convention as `run_osascript` there).

    test_mail.py and test_mail_drafts.py: for each of the 13 tests that reach `runtime.body_file`,
    pick one: (a) if the test asserts tempfile semantics (content written, file removed after the
    call, removed on error), restore the real function explicitly with
    `monkeypatch.setattr(runtime, "body_file", _real_body_file)` where `_real_body_file` is bound in
    the module-level import block (`from macos_apps_mcp.runtime import body_file as _real_body_file`);
    (b) otherwise fake it with the existing `_patch_body_file` helper (test_mail.py) or an
    equivalent `nullcontext` fake. A tempfile write reaches no app, so (a) is safe — but it must be
    explicit per test, never a module-wide exemption.

    test_native_seam.py: add `test_unit_tests_cannot_reach_a_seam_unfaked`, parametrized over the
    three names, asserting the AssertionError and its message; and
    `test_a_test_fake_overrides_the_lock` (monkeypatch `runtime.body_file` with a sentinel fake and
    assert `runtime.body_file is` that fake). Write these first and watch the body_file case fail
    before the conftest edit (RED), then make it pass (GREEN).

    Push the lane branch at the end (`git push -u origin HEAD`) so plan 01-04 continues from it.
    No PR yet.
  </action>
  <verify>
    <automated>uv run pytest tests/test_native_seam.py tests/test_runtime.py tests/test_mail.py tests/test_mail_drafts.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>uv run pytest -q && uv run ruff check . && uv run ruff format --check .</automated>
    <fails_when>non-zero exit from any of the three commands</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "body_file" tests/conftest.py` shows body_file in the refused seam names
    - `uv run pytest tests/test_native_seam.py -q -k "cannot_reach_a_seam_unfaked"` reports 3 passed
    - `uv run pytest tests/test_native_seam.py -q -k "fake_overrides_the_lock"` reports 1 passed
    - `uv run pytest -q` exits 0 with the three-name lock active
    - `git ls-remote --heads origin refactor/gate-card-1-native-seam` prints one line
  </acceptance_criteria>
  <done>All three seam names fail closed at runtime; every test that needs a seam fakes it or binds the real one explicitly; the lane branch is pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unit test → native seam | a test run on the owner's Mac must never reach Mail, Notes, Shortcuts or a live process table |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-05 | Tampering | a unit test that forgets a fake dials a real app (writes/sends/runs against live user data) | high | mitigate | conftest `_no_real_osascript` refuses run_osascript, body_file and tracked_run; lock self-tests in tests/test_native_seam.py prove each refusal |
| T-1-06 | Elevation of privilege | a module-wide exemption silently re-opens the seam | medium | mitigate | real-function use is per test and explicit (bound at import, restored by monkeypatch in that test only); the prohibition above is checked at code review |
| T-1-07 | Information disclosure | doctor tests read the dev machine's process table | low | mitigate | module-level fake of runtime.tracked_run in both doctor test files |
</threat_model>

<verification>
- The 36 tests measured failing under the three-name lock all pass.
- `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .` pass on the lane.
</verification>

<success_criteria>
GATE-01's runtime half is in place: run_osascript, body_file and tracked_run are each locked once
in conftest, and the suite is green with the lock on.
</success_criteria>

## Artifacts this phase produces

- `tests/conftest.py::_no_real_osascript` — extended to refuse `body_file` and `tracked_run`
- `tests/test_shortcuts.py::test_unfaked_tracked_run_is_refused`
- `tests/test_native_seam.py::test_unit_tests_cannot_reach_a_seam_unfaked`, `::test_a_test_fake_overrides_the_lock`
- `tests/test_doctor.py::_no_live_process_probe`, `tests/test_doctor_deploy.py::_no_live_process_probe` (module autouse fixtures)
- Module-level bindings `_real_tracked_run` (tests/test_shortcuts.py), `_real_body_file` (tests/test_mail.py, tests/test_mail_drafts.py where needed)
- Branch `refactor/gate-card-1-native-seam` (lane `.worktrees/gate-card-1-native-seam`)

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-02-SUMMARY.md` when done.
</output>
