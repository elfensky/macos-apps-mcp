---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 08
type: execute
wave: 5
depends_on: ["01-05", "01-06"]
files_modified: []
autonomous: false
requirements: [GATE-02, GATE-09, GATE-13]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "uv run pytest -m integration -k \"request_access or create_event or create_reminder\" is green on this Mac against develop with card 7 merged (GATE-02 device proof)"
    - "On the scratch mailbox, with the Mail watchdog running, a real move → mail_undo round trip returns every message to its source and leaves none in macos-apps-mcp-test"
    - "On real INBOX ids, move_mail and trash_mail dry-run envelopes and the osascript argv they send are identical between the v0.11.0 tree and the card-4 tree; dedupe_batch's dry run shows the one intended delta (a _PRESENT read, statuses from it)"
    - "Card 4 is merged only after the device verification passed, by one rebase-merged PR (D-07; CLAUDE.md: every Mail write verified on device)"
  artifacts: []
  key_links:
    - from: "card-4 lane (.worktrees/gate-card-4-recoverable-preflight)"
      to: "baseline worktree (.worktrees/baseline-v0.11.0)"
      via: "same real message ids driven through both trees with a recording pass-through around runtime.run_osascript"
      pattern: "run_osascript"
  prohibitions:
    - statement: "MUST NOT run a Mail write on device without the watchdog LaunchAgent loaded and logging, and never leave a polling loop pointed at Mail"
      status: resolved
      verification: judgment
    - statement: "MUST NOT move a real message anywhere except into the scratch mailbox macos-apps-mcp-test and back"
      status: resolved
      verification: judgment
---

<objective>
One owner device session covering two D-08 stops that fall at the same point in the landing order:
- D-08 stop 2 — card 7's EventKit device proof (GATE-02), on develop after card 7 merged. Card 5
  (plan 01-09) waits for it.
- D-08 stop 3 — card 4's scratch-mailbox verification with the Mail watchdog running (GATE-09),
  on the card-4 branch BEFORE it merges; then the card-4 merge.

Purpose: the refactors that touch EventKit and the destructive Mail plane are proven on the real
apps, not only in the suite (CLAUDE.md, docs/mail-applescript-facts.md §1).
Output: green integration selectors, device dry-run identity output, card 4 merged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-05-SUMMARY.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-06-SUMMARY.md
@docs/mail-applescript-facts.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Owner go for the device session (D-08 stops 2 and 3)</name>
  <decision>Run the EventKit integration selector and the Mail scratch-mailbox verification on this Mac now.</decision>
  <context>
    EventKit: the selector creates real events/reminders titled `macos-apps-mcp-test:` in the default
    calendar/list and deletes them; a TCC prompt may appear for the terminal process. Mail: the
    `move_then_undo` integration test moves up to two real INBOX messages into the scratch mailbox
    `macos-apps-mcp-test` and undoes the move; dry runs send only `_PRESENT` reads. The Mail watchdog
    LaunchAgent (`ren.lav.mail-watchdog`) must be loaded; if Mail hangs, run
    `~/mail-watchdog/capture.sh` BEFORE any force-quit (facts §9). Mail must be open and idle.
  </context>
  <options>
    <option id="run-now">
      <name>Run the device session now (recommended)</name>
      <pros>Unblocks card 5 and the card-4 merge.</pros>
      <cons>Needs the owner at the Mac for TCC prompts and to watch Mail.</cons>
    </option>
    <option id="later">
      <name>Later</name>
      <pros>No device activity now.</pros>
      <cons>Card 5, card 2 and the card-4 merge wait.</cons>
    </option>
  </options>
  <resume-signal>Type "run-now" or "later"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Card 7 EventKit device proof on develop</name>
  <precondition>Task 1 answered "run-now"; card 7 is merged on origin/develop (plan 01-06 SUMMARY records the merged sha).</precondition>
  <files>(none — runs the integration suite)</files>
  <read_first>
    - tests/test_integration.py (module header: TITLE_PREFIX, cleanup fixtures; the tests matching the selector)
    - .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-06-SUMMARY.md (merged sha)
  </read_first>
  <action>
    Per GATE-02 / D-08 stop 2. `git fetch -q origin`; `git worktree add --detach .worktrees/proof-card-7 origin/develop`;
    `uv sync` there; run `uv run pytest -m integration -k "request_access or create_event or create_reminder" -v`.
    Confirm the selector picked ≥ 3 tests (a selector matching nothing passes vacuously). On red:
    stop the phase here, report the failing test output, and open a fix-forward PR before card 5
    starts. Remove the worktree afterwards (`git worktree remove .worktrees/proof-card-7`).
  </action>
  <verify>
    <automated>uv run pytest -m integration -k "request_access or create_event or create_reminder" -q</automated>
    <fails_when>non-zero exit, or "no tests ran" / "0 passed" / "deselected" with no passes in the summary line, or fewer than 3 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - The summary line shows ≥ 3 passed and 0 failed (pasted into the SUMMARY with the develop sha tested)
    - No `macos-apps-mcp-test:` event or reminder remains afterwards (the tests' cleanup ran — checked with `uv run python -c` reading `CalendarAdapter().get_pointers("today")`/`RemindersAdapter` for the prefix, output pasted)
  </acceptance_criteria>
  <done>The runtime split is proven on real EventKit/TCC; card 5 may start.</done>
</task>

<task type="auto">
  <name>Task 3: Card 4 scratch-mailbox verification with the watchdog, device dry-run identity, then merge card 4</name>
  <precondition>`launchctl list | grep ren.lav.mail-watchdog` prints a line and `~/mail-watchdog/watchdog.log` grew within the last 60 s; Mail is running; the card-4 PR (plan 01-05) is open and review-clean.</precondition>
  <files>(none — verification and merge; a fix, if one is needed, goes back through plan 01-05's files on the card-4 lane)</files>
  <read_first>
    - docs/mail-applescript-facts.md (§1, §2 timing, §5c move/trash/undo facts, §8 bounded waits, §9 freezes)
    - tests/test_integration.py (fixtures `scratch_mailbox`, `inbox_messages`; tests `test_move_mail_dry_run_reads_mail_and_moves_nothing`, `test_move_then_undo_returns_every_message_to_its_source` ~1491-1575)
    - tests/test_mail_recover_dry_run_identity.py on the card-4 lane (the recording fake pattern)
    - .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-05-SUMMARY.md (PR number)
  </read_first>
  <action>
    Per GATE-09 / D-08 stop 3. In `.worktrees/gate-card-4-recoverable-preflight`: `git fetch -q origin`;
    if the branch is behind `origin/develop`, `git rebase origin/develop`, re-run
    `uv run pytest && uv run ruff check . && uv run ruff format --check .`, `git push --force-with-lease`,
    and wait for CI (`gh pr checks --watch --required`).

    (a) Real round trip on the scratch mailbox, from the card-4 lane:
    `uv run pytest -m integration -k "move_mail_dry_run or move_then_undo" -v`. Then INSPECT the result,
    not the return value (CLAUDE.md): read back with the adapter that each moved message id is again
    in its source mailbox and that `macos-apps-mcp-test` holds none of them; paste the read-back.

    (b) Device dry-run identity: pick two real INBOX message ids (the `inbox_messages` fixture's
    method) and the scratch mailbox. Write a scratch script outside the repo that installs a
    recording pass-through around `runtime.run_osascript` (records script name by identity, args,
    kwargs, then calls the real function) and runs `move_mail(ids, INBOX, scratch, dry_run=True)`,
    `trash_mail(ids, INBOX, dry_run=True)` and `dedupe_batch(ids, INBOX, dry_run=True)`, printing
    envelopes + calls as JSON. Run it once with the baseline environment
    (`uv run --project .worktrees/baseline-v0.11.0 python <script>`) and once with the card-4 lane
    (`uv run --project .worktrees/gate-card-4-recoverable-preflight python <script>`); diff the two
    outputs. Move and trash must be identical; dedupe must differ only by the `_PRESENT` call and the
    statuses it produced. Dry runs send only `_PRESENT` reads — no message moves. Paste the diff.

    If (a) or (b) fails: do not merge; fix on the card-4 lane (plan 01-05's files), re-run the
    triple, CI and review, then repeat this task.

    Merge (D-07): confirm CI green and the review still clean on the current head;
    `gh pr merge <N> --rebase --delete-branch`. Clean up: remove `.worktrees/gate-card-4-recoverable-preflight`
    and `.worktrees/baseline-v0.11.0` (unlock first), `git branch -D refactor/gate-card-4-recoverable-preflight`,
    `git fetch -q --prune origin`. Journal the landing (`vault-journal`). SUMMARY first line = one-line
    owner summary.
  </action>
  <verify>
    <automated>uv run pytest -m integration -k "move_mail_dry_run or move_then_undo" -q</automated>
    <fails_when>non-zero exit, or fewer than 2 passed, or "no tests ran" in the summary line</fails_when>
    <automated>gh pr view refactor/gate-card-4-recoverable-preflight --json state --jq .state</automated>
    <fails_when>output is anything other than MERGED</fails_when>
  </verify>
  <acceptance_criteria>
    - Watchdog precondition output (`launchctl list | grep ren.lav.mail-watchdog` and the log's last timestamp) pasted into the SUMMARY
    - The integration run shows ≥ 2 passed, 0 failed; the read-back shows every moved id back in its source mailbox and none in `macos-apps-mcp-test`
    - The device identity diff shows no difference for move and trash, and only the `_PRESENT` call + statuses for dedupe
    - The card-4 PR state is `MERGED` via rebase; both worktrees removed
  </acceptance_criteria>
  <done>Card 4 is verified on a real mailbox and landed; card 7 is proven on real EventKit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test run → the owner's real Calendar, Reminders and Mail | device tests act on live user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-21 | Tampering | real INBOX messages moved during verification | high | mitigate | only into `macos-apps-mcp-test` and back through the recoverable plane's undo; read-back inspection of the result; dry runs send only presence reads |
| T-1-22 | Denial of service | Mail wedges during the session and evidence is lost | medium | mitigate | watchdog precondition; `capture.sh` before any force-quit (facts §9); no polling loop at Mail |
| T-1-23 | Tampering | EventKit test items left in the owner's calendar | low | mitigate | `macos-apps-mcp-test:` prefix + the integration fixtures' cleanup; post-run read-back for leftovers |
| T-1-24 | Repudiation | card 4 merged without device proof | high | mitigate | merge happens only in this task, after (a) and (b) pass; blocking-human go at Task 1 |
</threat_model>

<verification>
- EventKit selector green with ≥ 3 tests; Mail round trip + identity diff as specified; card 4 merged.
</verification>

<success_criteria>
GATE-02's device proof and GATE-09's device verification (success criterion 5's device clause) are
done, at their D-08 points in the landing order.
</success_criteria>

## Artifacts this phase produces

- No new code symbols. Card-4 PR merged; worktrees `.worktrees/proof-card-7`, `.worktrees/gate-card-4-recoverable-preflight`, `.worktrees/baseline-v0.11.0` removed.

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-08-SUMMARY.md` when done.
</output>
