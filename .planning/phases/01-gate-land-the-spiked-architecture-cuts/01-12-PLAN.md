---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 12
type: execute
wave: 9
depends_on: ["01-11"]
files_modified:
  - macos_apps_mcp/server.py
  - tests/test_tool_annotations.py
  - tests/test_registry.py
  - macos_apps_mcp/notices.py
  - tests/test_server.py
  - macos_apps_mcp/registry.py
  - macos_apps_mcp/tiers.py
  - macos_apps_mcp/doctor.py
  - tests/test_doctor_deploy.py
  - tests/test_gate_on_dispatch.py
autonomous: true
requirements: [GATE-03, GATE-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every tool record states its adapter and the macOS permission(s) its docstring must name; tests/test_tool_annotations.py derives its permission table, additive/destructive sets and snapshot expectations from registry.TOOLS instead of hand tables"
    - "The meta tools ping, now, doctor and usage carry adapter None, permission () and notice False; audit carries adapter None and permission () but keeps the notice (its entries embed user-store args); every other tool carries an adapter (GATE-04 empty edge)"
    - "The untrusted-data notice and the #163 backup advisory are decided per call from the tool's record (notice, backup_notice); a tool with no record gets the notice (fail safe)"
    - "The outbound ledger has ONE owner: registry.outbound_status() derives registered and configured adapters from send records; tiers.py holds only read_only() and allow_send()"
    - "doctor reports outbound registered/configured/outbound_pending from registry.outbound_status(); after a toggle flip without restart the two lists still disagree and doctor reports outbound_pending"
  artifacts:
    - path: "macos_apps_mcp/registry.py"
      provides: "outbound_status() view over send records"
      contains: "def outbound_status"
    - path: "macos_apps_mcp/notices.py"
      provides: "UntrustedDataNotice reading notice/backup_notice from registry.TOOLS per call"
      contains: "registry"
    - path: "macos_apps_mcp/tiers.py"
      provides: "pure gate predicates only"
      contains: "def allow_send"
  key_links:
    - from: "macos_apps_mcp/doctor.py::diagnose"
      to: "macos_apps_mcp/registry.py::outbound_status"
      via: "from . import registry — the one ledger"
      pattern: "registry\\.outbound_status\\(\\)"
    - from: "macos_apps_mcp/notices.py::UntrustedDataNotice.on_call_tool"
      to: "macos_apps_mcp/registry.py::TOOLS"
      via: "per-call record lookup for notice and backup_notice"
      pattern: "registry\\.TOOLS"
  prohibitions:
    - statement: "MUST NOT keep a second outbound ledger — tiers.py loses _SEND_ADAPTERS, _SEND_REGISTERED, admit_send and outbound_status"
      status: resolved
      verification: test
---

<objective>
Card 2, part B (GATE-04, GATE-03): the record states every per-tool fact the rest of the system
read from hand-maintained tables — adapter, permission, notice, backup notice — and becomes the ONE
outbound ledger. This resolves RESEARCH Pitfall 3: card 5 put the ledger in `tiers.py`
provisionally; here the registry owns it and `tiers.py` is reduced to the pure gate predicates.
Continues on the card-2 lane; the PR lands in plan 01-13.

Recipe: spike 2's second commit `d9926c9` ("permission= and adapter= on every call site; tests read
the registry") — `git show d9926c9 -- macos_apps_mcp/server.py tests/test_tool_annotations.py tests/test_registry.py`.
Its AST-derived `_mail_tools()` oracle in test_tool_annotations stays as an independent check that
must agree with `adapter="mail"`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-RESEARCH.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-11-SUMMARY.md
@AGENTS.md

Lane: continue in `.worktrees/gate-card-2-registration-record`. First `git fetch -q origin`; if
`origin/develop` moved, `git rebase origin/develop` and re-run `uv run pytest`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: adapter= and permission= on every call site; test_tool_annotations reads the record (tracer: call site → record → docstring-permission test)</name>
  <files>macos_apps_mcp/server.py, tests/test_tool_annotations.py, tests/test_registry.py</files>
  <read_first>
    - tests/test_tool_annotations.py (the hand permission table ~120-140 incl. tuple permissions like mail_search → ("Full Disk Access", "Automation"); `_ADDITIVE_TOOLS`, `_DESTRUCTIVE_TOOLS`, `envelope_only`, `_RECOVERABLE_MAIL_TOOLS`, `_mail_tools()`)
    - macos_apps_mcp/server.py (every decorator as plan 01-11 left it)
    - `git show d9926c9 -- macos_apps_mcp/server.py tests/test_tool_annotations.py tests/test_registry.py`
  </read_first>
  <action>
    Per GATE-04: give every `_tool` alias call site `adapter="<name>"` (the adapter module its body
    dispatches to: reminders, calendar, contacts, mail, notes, safari, music, photos, messages,
    shortcuts) and `permission=` (the grant string or tuple the current test table records for that
    tool — "EventKit", "Automation", "Full Disk Access", "Shortcuts CLI", or a tuple). Meta tools
    (ping, now, doctor, usage, audit) carry no adapter and no permission. Copy the table values from
    the current tests/test_tool_annotations.py — do not re-derive them.

    tests/test_tool_annotations.py: the hand permission table, the additive/destructive sets and the
    snapshot expectation become views over `registry.TOOLS` (permission from `rec.permission`, tier
    from `rec.tier`, snapshot from `rec.snapshot`); keep the docstring-names-its-permission assertion
    and the AST `_mail_tools()` oracle, now asserting agreement with `registry.by_adapter("mail")`.
    tests/test_registry.py: freeze the develop-era tables as literals (copied from the current test
    file before editing it) and pin the derived views against them — so the move changes no fact;
    add `test_meta_tools_have_no_adapter_permission_or_notice` (ping, now, doctor, usage: adapter None,
    permission (), notice False; audit: adapter None, permission (), notice True) and
    `test_every_other_tool_names_its_adapter`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_tool_annotations.py tests/test_registry.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>MACOS_APPS_ALLOW_SEND=mail uv run pytest tests/test_tool_annotations.py tests/test_registry.py -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Every `@_read_tool`/`@_write_tool`/`@_additive_tool`/`@_send_tool` call site except the five meta tools carries `adapter=` (checked by `test_every_other_tool_names_its_adapter`)
    - tests/test_tool_annotations.py holds no hand-written permission dict (the frozen literal lives only in tests/test_registry.py as the pin)
    - Both commands above exit 0
  </acceptance_criteria>
  <done>Adapter and permission are stated once per tool and the annotation tests read them from the record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Notice and backup-advisory decided from the record, per call</name>
  <files>macos_apps_mcp/notices.py, macos_apps_mcp/server.py, tests/test_server.py, tests/test_registry.py</files>
  <read_first>
    - macos_apps_mcp/notices.py (as card 5 left it: `NO_NOTICE_TOOLS`, `_BACKUP_NOTICE_TOOLS`, `UntrustedDataNotice.on_call_tool`)
    - tests/test_server.py (notice tests; references to `notices.NO_NOTICE_TOOLS`)
    - macos_apps_mcp/registry.py (`no_notice`, `backup_notice_tools` views)
  </read_first>
  <behavior>
    - Test: a read tool's result gets the notice; ping/now/doctor/usage results do not; move_mail/trash_mail/mail_undo results get the advisory when backup_advisory() returns text
    - Test: a tool name absent from registry.TOOLS gets the notice (fail safe)
    - Test: registry.no_notice() == {"ping", "now", "doctor", "usage"} and registry.backup_notice_tools() == {"move_mail", "trash_mail", "mail_undo"}
  </behavior>
  <action>
    Per GATE-04 (notice exemption derived from the record): `UntrustedDataNotice.on_call_tool` looks up
    `registry.TOOLS.get(name)` per call — notice unless the record says `notice=False`; backup advisory
    when the record says `backup_notice=True`; no record → notice (fail safe). Delete
    `NO_NOTICE_TOOLS` and `_BACKUP_NOTICE_TOOLS` from notices.py (keep `UNTRUSTED_NOTICE`). The
    middleware keeps no constructor state, so its wiring position in server.py stays unchanged.
    server.py: `backup_notice=True` on `move_mail`, `trash_mail`, `mail_undo`. tests/test_server.py:
    point notice-set references at `registry.no_notice()`; add the behavior tests above (the set
    equalities go in tests/test_registry.py).
  </action>
  <verify>
    <automated>uv run pytest tests/test_server.py tests/test_registry.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^(NO_NOTICE_TOOLS|_BACKUP_NOTICE_TOOLS)" macos_apps_mcp/notices.py` prints nothing
    - `uv run pytest tests/test_server.py -q -k notice` passes, including the fail-safe case
  </acceptance_criteria>
  <done>The notice exemption and the backup advisory are record facts, read per call.</done>
</task>

<task type="auto">
  <name>Task 3: The registry becomes the only outbound ledger; tiers.py keeps only the gate predicates</name>
  <files>macos_apps_mcp/registry.py, macos_apps_mcp/tiers.py, macos_apps_mcp/server.py, macos_apps_mcp/doctor.py, tests/test_doctor_deploy.py, tests/test_gate_on_dispatch.py</files>
  <read_first>
    - macos_apps_mcp/tiers.py (provisional ledger: `_SEND_ADAPTERS`, `_SEND_REGISTERED`, `admit_send`, `outbound_status`)
    - macos_apps_mcp/server.py (`_tool` send branch calling `tiers.admit_send`)
    - macos_apps_mcp/doctor.py (`diagnose()` deployment section reading `tiers.outbound_status()`)
    - tests/test_doctor_deploy.py (tests of outbound_pending / off-when-read-only), tests/test_gate_on_dispatch.py (subprocess string printing the outbound status)
  </read_first>
  <action>
    Atomic by necessity (moving the ledger touches its producer, its consumer and their tests
    together). Per GATE-03 + GATE-04 (one ledger — RESEARCH Pitfall 3 resolved): add
    `registry.outbound_status()` returning `{"registered": sorted adapters of send records with
    registered=True, "configured": sorted adapters of send records for which tiers.allow_send(adapter)
    is true right now}` with the docstring carried from the old function (the C6 divergence story;
    doctor calls it only after the package imported server, which `macos_apps_mcp/__init__.py`
    guarantees). Delete `_SEND_ADAPTERS`, `_SEND_REGISTERED`, `admit_send` and `outbound_status` from
    tiers.py and fix its module docstring. server.py `_tool` send branch: `registered =
    tiers.allow_send(adapter)` (the record's adapter + registered flag ARE the ledger). doctor.py:
    `from . import deploy, registry, runtime` (drop `tiers` if unused) and read
    `registry.outbound_status()`. Tests: point every ledger reference at `registry` (including the
    test_gate_on_dispatch subprocess string); add to tests/test_doctor_deploy.py (or keep, if present)
    the case "configured but not registered → outbound_pending"; `tests/test_import_layers.py` must
    stay green unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_doctor_deploy.py tests/test_gate_on_dispatch.py tests/test_import_layers.py tests/test_registry.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>uv run pytest -q && MACOS_APPS_ALLOW_SEND=mail uv run pytest -q && uv run ruff check . && uv run ruff format --check .</automated>
    <fails_when>non-zero exit from any command</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "_SEND_ADAPTERS|_SEND_REGISTERED|def admit_send|def outbound_status" macos_apps_mcp/tiers.py` prints nothing
    - `grep -n "def outbound_status" macos_apps_mcp/registry.py` prints one line; `grep -n "registry.outbound_status()" macos_apps_mcp/doctor.py` prints one line
    - Both full-suite runs exit 0; the lane is pushed (`git push`)
  </acceptance_criteria>
  <done>One ledger: the registration record. tiers.py is the pure gate policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy config → outbound tool registration | whether send tools exist |
| tool result → model context | untrusted-data notice on user-store content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-34 | Spoofing | a tool loses the prompt-injection notice because its exemption is now derived | medium | mitigate | fail-safe default (no record → notice); set-equality test pins exactly four exempt meta tools |
| T-1-35 | Repudiation | doctor's outbound report disagrees with what is actually registered | medium | mitigate | one ledger derived from the records that drove registration; outbound_pending test after a toggle flip |
| T-1-36 | Elevation of privilege | send gate consulted differently after the ledger move | high | mitigate | `_tool` send branch calls tiers.allow_send at registration; FastMCP-vs-records test from plan 01-11 runs in ALLOW_SEND=mail and default modes |
</threat_model>

<verification>
- `uv run pytest` green in default and ALLOW_SEND=mail modes; layering test green.
</verification>

<success_criteria>
Annotations, gates, guard, snapshot registry, notice exemption, audit verb, outbound ledger and the
tests' expectations all read from one record (GATE-04), and doctor reads the ledger from below
server (GATE-03).
</success_criteria>

## Artifacts this phase produces

- `macos_apps_mcp/registry.py::outbound_status` (moved from tiers.py, now a view over send records)
- Removed: `tiers._SEND_ADAPTERS`, `tiers._SEND_REGISTERED`, `tiers.admit_send`, `tiers.outbound_status`, `notices.NO_NOTICE_TOOLS`, `notices._BACKUP_NOTICE_TOOLS`
- `tests/test_registry.py::test_meta_tools_have_no_adapter_permission_or_notice`, `::test_every_other_tool_names_its_adapter`, frozen develop-era table pins
- `tests/test_server.py` notice fail-safe test

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-12-SUMMARY.md` when done.
</output>
