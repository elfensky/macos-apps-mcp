---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_applescript_timeout.py
  - macos_apps_mcp/adapters/mail.py
autonomous: true
requirements: [GATE-10]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A tripwire test resolves every run_osascript call site in macos_apps_mcp/adapters/*.py and macos_apps_mcp/doctor.py to (template, host timeout) and fails when the template's with-timeout backstop is below the host cap"
    - "mail._DEDUPE's run-block backstop is ≥ 900 s and _DEDUPE_TIMEOUT stays 900.0"
    - "A backstop exactly equal to the host cap passes (≥, not >): doctor._PROBE at 120 s vs 120 s host is green (GATE-10 adjacency edge)"
    - "A call site with no timeout= keyword is checked against runtime._OSASCRIPT_TIMEOUT (30 s); a template with no with-timeout block already fails test_every_osascript_template_carries_with_timeout (GATE-10 empty edge)"
    - "The backstop is the integer N parsed from 'with timeout of N seconds' and compared numerically with the float host cap — never as text (GATE-10 encoding edge)"
    - "When a template holds several with-timeout blocks, the backstop is the first one at or after its 'on run' line, or the first in the template when it has no 'on run' — a 120 s handler block before a 600 s run block does not count (GATE-10 ordering edge)"
    - "A call site whose template or timeout expression the tripwire cannot resolve fails the test and names file:line — it is never skipped"
  artifacts:
    - path: "tests/test_applescript_timeout.py"
      provides: "the GATE-10 backstop ≥ host-cap tripwire"
      contains: "on run"
    - path: "macos_apps_mcp/adapters/mail.py"
      provides: "_DEDUPE template with a 900 s run-block backstop"
      contains: "with timeout of 900 seconds"
  key_links:
    - from: "tests/test_applescript_timeout.py"
      to: "macos_apps_mcp/adapters/*.py run_osascript call sites"
      via: "ast walk of every call whose func is run_osascript; template and timeout resolved from module attributes"
      pattern: "run_osascript"
  prohibitions:
    - statement: "MUST NOT fix a timeout inversion by lowering a host cap — _DEDUPE_TIMEOUT stays 900.0"
      status: resolved
      verification: test
---

<objective>
Card 9 (GATE-10): land ONLY the script-timeout tripwire plus the `_DEDUPE` fix. Spike 9's
`text.framed_script` wrapper was withdrawn at review and does not land (CONTEXT). The tripwire is
built fresh: spike 9 has no test for it (RESEARCH Pitfall 5, Assumption A1).

Measured before planning (scratch AST probe over current develop, 52 call sites): with the backstop
read as the `on run` block's first `with timeout`, exactly ONE inversion exists — `mail.py`
`_DEDUPE`: script 600 s < host `_DEDUPE_TIMEOUT` 900 s. `_MOVE`/`_TRASH`/`_SAVE_ATTACHMENT` carry a
120 s handler block (MAILBOX_REF) before their 600 s run block — that handler bounds one mailbox
lookup, not the call, so it is not the backstop. One call site takes its template by attribute
(`mail.py`: `mail_drafts._DELETE_DRAFT`); the resolver must follow `module.NAME`.

Purpose: the backstop invariant (script ≥ host) is stated once and read back, not re-derived per
template — manual review already missed `_DEDUPE` once.
Output: tripwire in `tests/test_applescript_timeout.py` (the #56 template test's home — no new
file), `_DEDUPE` backstop 900 s, card-9 PR merged.

Cross-phase note: Phase 02.1 (MAIL-01) will make `_MOVE`/`_TRASH`/`_PRESENT` host timeouts scale
with batch size. The tripwire fails closed on an unresolvable timeout expression, so that phase
must extend it deliberately.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-RESEARCH.md
@docs/mail-applescript-facts.md
@AGENTS.md

Read `docs/mail-applescript-facts.md` FIRST (CLAUDE.md: any Mail change). Lane:
`.worktrees/gate-card-9-timeout-tripwire`, branch `fix/gate-card-9-timeout-tripwire`, cut from
`origin/develop` after plan 01-01 merged. Runs in parallel with plan 01-02 (no shared file). Do
not edit `pyproject.toml`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Backstop ≥ host-cap tripwire over every call site; fix _DEDUPE (tracer: test → template → call site)</name>
  <files>tests/test_applescript_timeout.py, macos_apps_mcp/adapters/mail.py</files>
  <read_first>
    - docs/mail-applescript-facts.md (§2 timing, §8 bounded waits, §9 freezes)
    - tests/test_applescript_timeout.py (current #56 template tests — `_osascript_templates()`)
    - tests/test_native_seam.py (the ast-walk tripwire shape to mirror)
    - macos_apps_mcp/adapters/mail.py (`_DEDUPE_TIMEOUT` ~line 468, `_DEDUPE` template ~499-520, its call site ~1484; `_MOVE`/`_TRASH` for the handler-plus-run-block shape)
    - macos_apps_mcp/runtime.py (`_OSASCRIPT_TIMEOUT`, `run_osascript` signature, and the comment on script-vs-host timeout ordering)
    - macos_apps_mcp/adapters/mail_triage.py (host 600 s call sites) and macos_apps_mcp/doctor.py (`_PROBE`, `_PROBE_TIMEOUT`)
  </read_first>
  <behavior>
    - Test: every discovered call site satisfies backstop ≥ host cap; the failure message names module:line, template name, backstop and host values
    - Test: discovery finds ≥ 40 call sites and includes (mail.py, _DEDUPE) and (doctor.py, _PROBE)
    - Test: before the mail.py edit, exactly the _DEDUPE call site fails (600 < 900); after it, all pass
  </behavior>
  <action>
    Create the lane (`git fetch -q --prune origin`;
    `git worktree add --lock --reason "$(hostname -s)" .worktrees/gate-card-9-timeout-tripwire -b fix/gate-card-9-timeout-tripwire origin/develop`; `uv sync`).

    In `tests/test_applescript_timeout.py` add a call-site resolver, `_call_sites()`, per GATE-10:
    parse (ast) every `macos_apps_mcp/adapters/*.py` (excluding `__init__.py`) plus
    `macos_apps_mcp/doctor.py`; for every `ast.Call` whose func is the name or attribute
    `run_osascript`, resolve (a) the template from the first positional arg — an `ast.Name` is
    looked up on the imported module, an `ast.Attribute` `mod.NAME` is looked up on the module
    object the importing module binds to `mod`; (b) the host cap from the `timeout=` keyword — an
    `ast.Constant` value, an `ast.Name` module attribute, or `runtime._OSASCRIPT_TIMEOUT` when the
    keyword is absent. Backstop rule: the integer N of the first `with timeout of N seconds` at or
    after the template's `on run` line; when the template has no `on run`, the first in the
    template. Anything unresolvable (template not a str, no backstop found, timeout expression of
    another shape) produces a failing case naming `file:line` — never a skip.

    Tests: `test_every_call_site_backstop_covers_host_cap` parametrized over the call sites
    (ids `module.py:line:TEMPLATE`, list sorted so ids are stable) asserting backstop ≥ host;
    `test_call_sites_are_discovered` asserting ≥ 40 sites and the presence of
    (`mail.py`, `_DEDUPE`) and (`doctor.py`, `_PROBE`). Run the suite and confirm the only failure
    is the `_DEDUPE` site (RED).

    Then fix, per CONTEXT discretion "_DEDUPE (card 9)": in `mail.py`'s `_DEDUPE` template change the
    run block's `with timeout of 600 seconds` to `with timeout of 900 seconds`. Do NOT change
    `_DEDUPE_TIMEOUT = 900.0`. If a comment near `_DEDUPE_TIMEOUT` or `_DEDUPE` states the old 600 s
    value, correct it. Re-run (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_applescript_timeout.py -v</automated>
    <fails_when>non-zero exit, or "no tests ran", or fewer than 40 "test_every_call_site_backstop_covers_host_cap[" lines in the verbose output</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "with timeout of 600 seconds" macos_apps_mcp/adapters/mail.py` shows no line inside the `_DEDUPE` template (the `_MOVE`/`_TRASH` 600 s blocks stay)
    - `grep -n "_DEDUPE_TIMEOUT = 900.0" macos_apps_mcp/adapters/mail.py` prints one line
    - `uv run pytest tests/test_applescript_timeout.py -q -k "backstop_covers_host_cap"` passes with ≥ 40 cases
    - Temporarily reverting the 900 back to 600 makes exactly one case fail (the `_DEDUPE` site) — observed during RED, recorded in the SUMMARY
  </acceptance_criteria>
  <done>The tripwire covers every call site, fails closed on anything it cannot resolve, and passes with _DEDUPE's backstop at 900 s.</done>
</task>

<task type="auto">
  <name>Task 2: Land card 9 (PR, CI, code review, rebase-merge, journal)</name>
  <files>(no new edits — landing only; review fixes, if any, stay within tests/test_applescript_timeout.py and macos_apps_mcp/adapters/mail.py)</files>
  <read_first>
    - AGENTS.md ("Worktrees — one lane, always")
    - docs/RELEASING.md ("Feature work")
    - .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md (D-07)
  </read_first>
  <action>
    Per D-07. In the lane: run the verify triple (`uv run pytest`, `uv run ruff check .`,
    `uv run ruff format --check .`). `git push -u origin HEAD`; `gh pr create --base develop` with
    title "test(mail): script-timeout tripwire + _DEDUPE backstop 900 s (gate card 9, GATE-10)" and
    a body stating: the tripwire rule (on-run backstop ≥ host cap, fail closed), the one inversion
    found and fixed, and that spike 9's `framed_script` wrapper was withdrawn and does not land.
    `gh pr checks --watch --required`. Run a code-review pass over `origin/develop...HEAD` (the
    `code-review` skill, Standards + Spec, spec = this plan + GATE-10); fix every finding, re-run the
    triple, push, repeat until the review reports no open issue. If the PR is behind `develop`
    (plan 01-02 may merge first), `git rebase origin/develop`, re-run the triple,
    `git push --force-with-lease`, and wait for CI again. Then `gh pr merge --rebase --delete-branch`.

    Clean up: `git worktree unlock` + `git worktree remove .worktrees/gate-card-9-timeout-tripwire` +
    `git branch -D fix/gate-card-9-timeout-tripwire`; `git fetch -q --prune origin`. Record the
    landing with the `vault-journal` skill (one bullet under #personal). The SUMMARY's first line
    is the one-line owner summary (D-07).
  </action>
  <verify>
    <automated>set -o pipefail; git fetch -q origin && git show origin/develop:macos_apps_mcp/adapters/mail.py | grep -c "with timeout of 900 seconds"</automated>
    <fails_when>non-zero exit, or the count prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh pr view <N> --json state` reports `MERGED`, merged with `gh pr merge --rebase` (develop stays linear)
    - CI `check` job green on the merged head
    - The code-review pass output ends with no open issue (summary pasted into the SUMMARY)
    - `git worktree list` lists no `.worktrees/gate-card-9-timeout-tripwire`
  </acceptance_criteria>
  <done>Card 9 is on origin/develop by a rebase-merged PR; the lane is removed; the owner has a one-line summary.</done>
</task>

</tasks>
<!-- plan-criteria-allow: R4 - the piped git verify starts with set -o pipefail, so the pipeline reports git's own exit status -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host process → Mail via osascript | the script's own `with timeout` is the backstop when the host cannot stop Mail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-08 | Denial of service | a script self-aborting before its host cap mid-batch leaves a destructive batch half done and unreported | medium | mitigate | tripwire asserts backstop ≥ host cap at every call site; `_DEDUPE` raised to 900 s |
| T-1-09 | Tampering | a future template added with an unresolvable timeout escapes the check | low | mitigate | unresolvable call sites fail the test and name file:line |
</threat_model>

<verification>
- `uv run pytest tests/test_applescript_timeout.py -v` passes with ≥ 40 call-site cases.
- `origin/develop` carries the `_DEDUPE` 900 s backstop with `_DEDUPE_TIMEOUT` unchanged.
</verification>

<success_criteria>
The script-timeout invariant is a test, the one known inversion is fixed without lowering a host
cap, and card 9 is landed.
</success_criteria>

## Artifacts this phase produces

- `tests/test_applescript_timeout.py::_call_sites`, `::test_every_call_site_backstop_covers_host_cap`, `::test_call_sites_are_discovered`
- `macos_apps_mcp/adapters/mail.py::_DEDUPE` — run-block backstop 900 s (string change only)
- Branch `fix/gate-card-9-timeout-tripwire` (deleted after merge)

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-03-SUMMARY.md` when done.
</output>
