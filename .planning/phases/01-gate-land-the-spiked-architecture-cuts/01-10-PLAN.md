---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 10
type: execute
wave: 7
depends_on: ["01-09", "01-07"]
files_modified: []
autonomous: false
requirements: [GATE-13]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A daemon rebuilt from origin/develop after card 5 answers doctor() with version 0.11.0 (no bump) and a build token that resolves to the develop commit it was built from, without -dirty (D-06)"
    - "One outbound check reports gated correctly: when doctor's deployment.outbound lists mail, send_mail with dry_run=true returns a dry_run preview; when it does not, send_mail is absent from the tool list"
    - "doctor's mail_index surface reports ok or sidecar on the real store — the widened HEADER_FINGERPRINT (card 3) holds on this Mac"
  artifacts: []
  key_links:
    - from: ".worktrees/.daemon_probe.py"
      to: "the running daemon"
      via: "fastmcp Client over the daemon's unix socket (deploy._request_grants_via_daemon pattern)"
      pattern: "_uds_client_factory"
  prohibitions:
    - statement: "MUST NOT send real mail during the outbound check — only a dry run, which makes no native call"
      status: resolved
      verification: judgment
---

<objective>
D-06 / D-08 stop 4 (first of two): after card 5 lands, rebuild the daemon from `develop` as a DEV
build (no version bump, no tag), reinstall, kickstart, and prove the gates still read correctly.
"The new version" in success criterion 6 means "the new build sha" here (D-06). Card 2 (plan 01-11)
waits for this check. Card 3 has landed by now (plan 01-07), so this check also proves its widened
fingerprint on the real Envelope Index through doctor.

Output: the probe output in the SUMMARY; the owner reconnects clients.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-01-SUMMARY.md
@docs/DAEMON.md
@docs/RELEASING.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Owner go for the post-card-5 dev-build daemon swap (D-08 stop 4)</name>
  <decision>Rebuild the daemon from origin/develop now (signed dev build, no notarization), reinstall, kickstart, and probe it.</decision>
  <context>
    The swap drops every connected MCP client's transport; each Claude Code session must reconnect
    (`/mcp`). Signing uses the login-keychain Developer ID; macOS may ask for the keychain password.
    Same Developer ID keeps the TCC grants. The installed build becomes a dev build of 0.11.0
    until the post-card-2 check and, later, the next release.
  </context>
  <options>
    <option id="swap-now">
      <name>Rebuild and swap now (recommended)</name>
      <pros>Card 2 can start on a proven gate.</pros>
      <cons>Connected sessions drop once.</cons>
    </option>
    <option id="later">
      <name>Later</name>
      <pros>No interruption now.</pros>
      <cons>Card 2 waits.</cons>
    </option>
  </options>
  <resume-signal>Type "swap-now" or "later"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dev build from develop, install, kickstart, and probe version, build, outbound gate and mail_index</name>
  <precondition>Task 1 answered "swap-now"; `security find-identity -v -p codesigning` lists "Developer ID Application: Andrei M. Lavrenov (VUMUR696L9)".</precondition>
  <files>(none in the repo — /Applications/macos-apps-mcp.app and the git-ignored .worktrees/.daemon_probe.py)</files>
  <read_first>
    - docs/DAEMON.md ("1. Build the signed .app", "2. Move it to /Applications", "The shim is per-connection")
    - macos_apps_mcp/deploy.py (`_request_grants_via_daemon`)
    - macos_apps_mcp/doctor.py (`diagnose()` keys: version, build, deployment.outbound, surfaces incl. mail_index)
    - .worktrees/.daemon_probe.py if it exists (written by plan 01-01)
  </read_first>
  <action>
    Per D-06. `git fetch -q origin`; `git worktree add --detach .worktrees/devbuild-card-5 origin/develop`;
    record `BUILT=$(git -C .worktrees/devbuild-card-5 rev-parse HEAD)`. Inside it:
    `scripts/build_app.sh --sign "Developer ID Application: Andrei M. Lavrenov (VUMUR696L9)" --out dist`
    (no `--notarize`: a local same-Mac build never carries quarantine). Install: remove
    `/Applications/macos-apps-mcp.app`, `cp -R dist/macos-apps-mcp.app /Applications/`,
    `launchctl kickstart -k gui/$(id -u)/ren.lav.macos-apps-mcp`, wait (bounded, ≤ 30 s) for the socket.

    Probe with `.worktrees/.daemon_probe.py` (write it if absent: fastmcp `Client` over
    `StreamableHttpTransport("http://daemon/mcp", httpx_client_factory=daemon._uds_client_factory(daemon.socket_path()))`;
    call `doctor` with `{"request": false}` and `list_tools`). Print `version`, `build`,
    `deployment.outbound`, the `mail_index` surface status, and whether `send_mail` is listed. Then
    the outbound check: if `deployment.outbound` contains "mail", call `send_mail` with
    `{"to": "andrei@lav.ren", "subject": "gate check", "body": "dry run", "dry_run": true}` and print
    the result (it must carry `dry_run: true`; a dry run makes no native call); otherwise assert
    `send_mail` is absent from `list_tools`. Pass criteria: version `0.11.0`; the build token's
    `git rev-parse` equals `$BUILT` and has no `-dirty`; outbound gated as described; mail_index
    `ok` or `sidecar`. Remove the worktree afterwards. Tell the owner to reconnect clients.
  </action>
  <verify>
    <automated>uv run python .worktrees/.daemon_probe.py</automated>
    <fails_when>the probe cannot connect, or prints a version other than 0.11.0, or a build token that does not resolve to the built develop commit, or "-dirty", or a mail_index status other than ok/sidecar, or send_mail listed while outbound is off (or a non-dry-run send result)</fails_when>
    <human-check>Owner reconnects one Claude Code session; its doctor() shows the same build token.</human-check>
  </verify>
  <acceptance_criteria>
    - The probe output (pasted into the SUMMARY with `$BUILT`) shows version 0.11.0, the matching build token without `-dirty`, the outbound branch taken and its result, and mail_index ok/sidecar
    - `git worktree list` lists no `.worktrees/devbuild-card-5`
  </acceptance_criteria>
  <done>The post-card-5 daemon runs develop's build and its gates read correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → launchd daemon with the owner's TCC grants | the installed bundle serves every MCP client |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-28 | Elevation of privilege | card 5 moved the gate and the daemon now registers send tools while outbound is off | high | mitigate | probe asserts send_mail absent when outbound is off, dry-run-only when on |
| T-1-29 | Tampering | a dirty or wrong tree is installed as the daemon | medium | mitigate | build in a detached worktree at origin/develop; build token must equal `$BUILT` without `-dirty` |
| T-1-30 | Spoofing | a re-sign with another identity breaks TCC grants | medium | mitigate | sign with the same Developer ID only (precondition) |
</threat_model>

<verification>
- Probe output meets every pass criterion; the owner has reconnected.
</verification>

<success_criteria>
Success criterion 6's post-card-5 daemon check holds (D-06 reading: the new build sha).
</success_criteria>

## Artifacts this phase produces

- No new code symbols. `.worktrees/.daemon_probe.py` (git-ignored helper) reused or created.

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-10-SUMMARY.md` when done.
</output>
