---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 06
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - macos_apps_mcp/eventkit.py
  - macos_apps_mcp/runtime.py
  - macos_apps_mcp/adapters/calendar.py
  - macos_apps_mcp/adapters/reminders.py
  - macos_apps_mcp/doctor.py
  - macos_apps_mcp/server.py
  - tests/test_eventkit.py
  - tests/test_runtime.py
  - tests/test_integration.py
  - macos_apps_mcp/contracts.py
  - macos_apps_mcp/lifecycle.py
  - DESIGN.md
autonomous: true
requirements: [GATE-02, GATE-13]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "runtime's public surface is exactly run_native, on_worker, app_process_info, terminate_children, tracked_run, run_osascript, body_file, verify_sqlite_schema, read_via_sqlite, mac_region, log — a test fails on any extra or missing public name"
    - "runtime imports no EventKit; the EventKit cluster (store, NSDate/RRULE coercion, TCC request, run_native_async, bootstrap) lives in macos_apps_mcp/eventkit.py, which calendar.py and reminders.py import from"
    - "Every moved function body is byte-identical to its text on develop before the cut, except store()'s fence call _on_worker() → on_worker() (GATE-02 encoding edge: equality by exact source text)"
    - "eventkit.py defines no executor; runtime._executor stays ThreadPoolExecutor(max_workers=1) and eventkit dispatches only through runtime.run_native (GATE-02 concurrency edge)"
    - "eventkit.bootstrap() stays non-fatal when EventKit access is denied or times out — the moved tests that pin it pass unchanged (GATE-02 empty edge)"
    - statement: "eventkit.store() called twice on the mac-native worker returns the same EKEventStore object — one process-wide store (GATE-02 idempotency edge)"
      verification: backstop
    - "Card 7 is on origin/develop by one rebase-merged PR (D-07)"
  artifacts:
    - path: "macos_apps_mcp/eventkit.py"
      provides: "EventKit plane: store, request_access, request_access_each, bootstrap, run_native_async, to_nsdate, epoch_nsdate, from_nsdate, due_components, to_recurrence_rule, recurrence_signature, persisted_recurrence_signature, rrule_text, container_id"
      contains: "def request_access_each"
    - path: "macos_apps_mcp/runtime.py"
      provides: "the native door only (~10 public names)"
      contains: "def on_worker"
    - path: "tests/test_eventkit.py"
      provides: "EventKit-plane unit tests moved out of test_runtime.py"
      contains: "from macos_apps_mcp.eventkit import"
  key_links:
    - from: "macos_apps_mcp/adapters/calendar.py, macos_apps_mcp/adapters/reminders.py"
      to: "macos_apps_mcp/eventkit.py"
      via: "from ..eventkit import (...); run_native still from ..runtime"
      pattern: "from \\.\\.eventkit import"
    - from: "macos_apps_mcp/eventkit.py::store"
      to: "macos_apps_mcp/runtime.py::on_worker"
      via: "thread-affinity fence — eventkit enforces, runtime owns the worker"
      pattern: "on_worker\\(\\)"
    - from: "macos_apps_mcp/server.py, macos_apps_mcp/doctor.py"
      to: "macos_apps_mcp/eventkit.py"
      via: "server imports bootstrap; doctor imports request_access_each"
      pattern: "from \\.eventkit import"
  prohibitions:
    - statement: "MUST NOT create a second EKEventStore or a second executor, or widen runtime._executor past max_workers=1, while moving the EventKit cluster"
      status: resolved
      verification: test
---

<objective>
Card 7 (GATE-02): split `runtime.py`. The EventKit cluster moves to a new
`macos_apps_mcp/eventkit.py`; `runtime.py` keeps only the native door (~10 public names).
Calendar and Reminders import the cluster from the one module `eventkit` (by-name imports — their
tests patch `cal.store` / `rem.store`, so this keeps those tests unchanged). Moved bodies are
copied from the lane's current `runtime.py` (develop after card 1), never from the spike. Then land
card 7 by PR. The device proof (`-m integration -k "request_access or create_event or create_reminder"`)
is the D-08 stop in plan 01-08; card 5 (plan 01-09) waits for it.

Purpose: the module every adapter imports stays small and EventKit-free; the file cards 5 and 2
edit next is settled.
Output: `eventkit.py`, trimmed `runtime.py`, re-pointed imports, split tests, a public-surface
test, card 7 merged.

Public-surface decisions carried from the spike recipe: `_on_worker` becomes public `on_worker`
(eventkit's `store()` needs the fence without owning the worker); `classify_stuck_app` →
`_classify_stuck_app` and `track_child` → `_track_child` (only runtime uses them). `log` stays in
runtime. `_open_sqlite_ro` stays private in runtime (conftest's `envelope_mode` and `deploy.py`
reach it by name — unchanged here).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-RESEARCH.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-PATTERNS.md
@AGENTS.md

Recipe: `git diff d9ac75f01eab732509324c0f91087bd587ee6242..spike/arch-review-7-runtime-split`
and `git show spike/arch-review-7-runtime-split:macos_apps_mcp/eventkit.py` (module docstring, import
block), `git show spike/arch-review-7-runtime-split:tests/test_eventkit.py`. Bodies come from the
lane's `runtime.py`, never from the spike.

Lane: `.worktrees/gate-card-7-runtime-split`, branch `refactor/gate-card-7-runtime-split`, cut from
`origin/develop` AFTER card 1 (plan 01-04) merged — card 1 changed `doctor.py`'s import block.
Record the lane's base commit (`git rev-parse origin/develop` at creation) as `BASE` in the SUMMARY;
the byte-identity check compares against `BASE`. Do not edit `pyproject.toml`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Move the EventKit cluster to eventkit.py and re-point every importer — one atomic move (tracer)</name>
  <files>macos_apps_mcp/eventkit.py, macos_apps_mcp/runtime.py, macos_apps_mcp/adapters/calendar.py, macos_apps_mcp/adapters/reminders.py, macos_apps_mcp/doctor.py, macos_apps_mcp/server.py, tests/test_eventkit.py, tests/test_runtime.py, tests/test_integration.py</files>
  <read_first>
    - macos_apps_mcp/runtime.py (whole file; the moved names are at lines ~67-131, ~379-420, ~587-744; internal `_on_worker()` uses at ~120, ~358, ~573)
    - macos_apps_mcp/adapters/calendar.py lines ~20-35 and macos_apps_mcp/adapters/reminders.py lines ~18-33 (import blocks)
    - macos_apps_mcp/doctor.py import block (as card 1 left it: `from . import deploy, runtime` / `from .runtime import app_process_info, request_access_each, run_native`)
    - macos_apps_mcp/server.py line ~45 (`from .runtime import bootstrap`)
    - tests/test_runtime.py (import block ~32-45 and every EventKit-typed test), tests/test_integration.py lines ~16-20, ~244, ~317, ~378
    - spike 7's eventkit.py and test_eventkit.py (see context) for module docstring and test grouping only
  </read_first>
  <action>
    Create the lane (`git fetch -q --prune origin`;
    `git worktree add --lock --reason "$(hostname -s)" .worktrees/gate-card-7-runtime-split -b refactor/gate-card-7-runtime-split origin/develop`; `uv sync`).

    This task is atomic by necessity: moving a name and re-pointing its importers must land
    together, or the suite goes red (a transient re-export shim in runtime would be scaffolding —
    forbidden). Hence 9 files in one task.

    Per GATE-02, create `macos_apps_mcp/eventkit.py` (module docstring: the EventKit plane; thread
    affinity is enforced here, owned by runtime; never create a second store or executor). Move,
    with bodies copied byte-for-byte from the lane's `runtime.py`: `_FULL_ACCESS`, `_ACCESS_TIMEOUT`,
    `container_id`, `_require_full_access`, `_store`, `store`, `_ASYNC_TIMEOUT`, `run_native_async`,
    `to_nsdate`, `epoch_nsdate`, `from_nsdate`, `due_components`, `_FREQUENCIES`, `to_recurrence_rule`,
    `recurrence_signature`, `persisted_recurrence_signature`, `_FREQUENCY_NAMES`, `rrule_text`,
    `_request_one`, `_ENTITIES`, `request_access`, `request_access_each`, `bootstrap`. eventkit
    imports `EventKit as EK`, `Foundation as F`, `datetime`, `threading`/`Callable`/`TypeVar` as the
    moved bodies need, `from .contracts import CLEAR_RECURRENCE, Recurrence`, the error classes they
    raise, and `from .runtime import log, on_worker, run_native`. The only body edit: `store()`'s
    `_on_worker()` becomes `on_worker()`.

    runtime.py: delete the moved names; drop `import EventKit as EK`, the `.contracts` import and the
    `datetime` import once nothing uses them (keep `Foundation as F` — `mac_region` uses it); rename
    `_on_worker` → `on_worker` (update its two internal call sites) with a docstring saying it is
    public so eventkit can fence the store; rename `classify_stuck_app` → `_classify_stuck_app` and
    `track_child` → `_track_child` (update internal call sites and docstring mentions).

    Importers: calendar.py and reminders.py import every moved name from `..eventkit` and keep
    `from ..runtime import run_native`; doctor.py `from .eventkit import request_access_each` and
    `from .runtime import app_process_info, run_native` (keep `from . import deploy, runtime`);
    server.py `from .eventkit import bootstrap`. Tests: create `tests/test_eventkit.py` holding
    every EventKit-typed test moved out of `tests/test_runtime.py` (imports from
    `macos_apps_mcp.eventkit`, `run_native` from runtime); in `tests/test_runtime.py` update
    `runtime.classify_stuck_app` → `runtime._classify_stuck_app`; `tests/test_integration.py`
    imports `request_access, store, to_nsdate` from `macos_apps_mcp.eventkit` and `run_native` from
    `macos_apps_mcp.runtime` (four import sites).
  </action>
  <verify>
    <automated>uv run pytest tests/test_eventkit.py tests/test_runtime.py tests/test_calendar.py tests/test_reminders.py tests/test_free_busy.py tests/test_doctor.py -q</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^import EventKit|^from \.contracts import" macos_apps_mcp/runtime.py` prints nothing
    - `grep -nE "^def (store|to_nsdate|request_access|request_access_each|bootstrap|run_native_async|rrule_text)\b" macos_apps_mcp/runtime.py` prints nothing; the same names are defined in macos_apps_mcp/eventkit.py
    - `grep -n "from \.\.eventkit import" macos_apps_mcp/adapters/calendar.py macos_apps_mcp/adapters/reminders.py` prints two lines
    - `grep -rn "from macos_apps_mcp.runtime import.*\(to_nsdate\|request_access\|store\)" tests/` prints nothing
    - `uv run pytest -q` exits 0
  </acceptance_criteria>
  <done>The EventKit cluster lives in eventkit.py, every importer points at it, and the suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the runtime public surface and the single worker; prove byte-identical bodies; fix stale docstrings</name>
  <files>tests/test_runtime.py, tests/test_eventkit.py, macos_apps_mcp/contracts.py, macos_apps_mcp/lifecycle.py, DESIGN.md</files>
  <read_first>
    - macos_apps_mcp/runtime.py and macos_apps_mcp/eventkit.py (as Task 1 left them)
    - macos_apps_mcp/contracts.py (`Recurrence` docstring, "runtime.to_recurrence_rule")
    - macos_apps_mcp/lifecycle.py (module docstring, "runtime.bootstrap()")
    - DESIGN.md (module map, the `runtime.py` line ~48)
  </read_first>
  <behavior>
    - Test: the set of public names runtime defines (callables and `log`, excluding imported modules and names starting with "_") equals the 11-name allowlist exactly
    - Test: runtime has no attribute `EK`; eventkit defines no ThreadPoolExecutor instance; runtime._executor._max_workers == 1
  </behavior>
  <action>
    Per GATE-02. tests/test_runtime.py: add `test_runtime_public_surface_is_the_native_door` — the
    public names are those in `vars(runtime)` that do not start with "_" and are either functions
    whose `__module__ == "macos_apps_mcp.runtime"` or the logger `log`; assert equality with
    `{"run_native", "on_worker", "app_process_info", "terminate_children", "tracked_run",
    "run_osascript", "body_file", "verify_sqlite_schema", "read_via_sqlite", "mac_region", "log"}` and
    `not hasattr(runtime, "EK")`. tests/test_eventkit.py: add `test_eventkit_owns_no_executor`
    (no attribute of eventkit is a `ThreadPoolExecutor`; `runtime._executor._max_workers == 1`).

    Byte-identity check (one-off, not a permanent test — future edits are allowed): a scratch
    script outside the repo parses `git show "$BASE":macos_apps_mcp/runtime.py` and the lane's
    `macos_apps_mcp/eventkit.py` with `ast`, extracts each moved top-level definition's source via
    `ast.get_source_segment`, and prints `OK <name>` or `DIFF <name>` for every moved name. Every
    name must print OK except `store`, whose only difference is `_on_worker()` → `on_worker()`
    (show that diff). Paste the output into the SUMMARY.

    Docstrings (text only): contracts.py `Recurrence` — `runtime.to_recurrence_rule` →
    `eventkit.to_recurrence_rule`; lifecycle.py — `runtime.bootstrap()` → `eventkit.bootstrap()`;
    DESIGN.md module map — the `runtime.py` line becomes "the single serialized native worker thread
    + osascript/sqlite dispatch" and add an `eventkit.py` line "EKEventStore, NSDate/RRULE coercion,
    TCC consent — on runtime's worker".
  </action>
  <verify>
    <automated>uv run pytest tests/test_runtime.py tests/test_eventkit.py -q -k "public_surface or owns_no_executor"</automated>
    <fails_when>non-zero exit, or fewer than "2 passed" in the summary line</fails_when>
    <automated>uv run pytest -q && uv run ruff check . && uv run ruff format --check .</automated>
    <fails_when>non-zero exit from any of the three commands</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_runtime.py -q -k public_surface_is_the_native_door` reports 1 passed
    - The byte-identity output in the SUMMARY lists OK for every moved name, and a single documented difference in `store`
    - `grep -n "runtime.to_recurrence_rule" macos_apps_mcp/contracts.py` and `grep -n "runtime.bootstrap" macos_apps_mcp/lifecycle.py` print nothing
    - `grep -n "eventkit.py" DESIGN.md` prints one line
  </acceptance_criteria>
  <done>The runtime surface and the single worker are pinned by tests; moved bodies are proven identical; docs name the new module.</done>
</task>

<task type="auto">
  <name>Task 3: Land card 7 (PR, CI, code review, rebase-merge, journal)</name>
  <files>(no new edits — review fixes, if any, stay within this plan's files)</files>
  <read_first>
    - AGENTS.md ("Worktrees — one lane, always"), .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md (D-07, D-08 step 2)
  </read_first>
  <action>
    Per D-07. Verify triple in the lane; `git push -u origin HEAD`; `gh pr create --base develop`
    titled "refactor: split runtime.py — EventKit plane in eventkit.py (gate card 7, GATE-02)"; body:
    the moved names, the 11-name surface, the byte-identity output, and "device proof is the D-08
    stop in plan 01-08; card 5 waits for it". `gh pr checks --watch --required`. Code-review pass over
    `origin/develop...HEAD` (`code-review` skill, spec = this plan + GATE-02); fix until none open.
    If behind develop: rebase onto `origin/develop`, re-run the triple, `git push --force-with-lease`,
    wait for CI. `gh pr merge --rebase --delete-branch`. Clean up the lane, `git fetch -q --prune origin`,
    journal the landing (`vault-journal`), SUMMARY first line = one-line owner summary. Record the
    merged develop sha for plan 01-08.
  </action>
  <verify>
    <automated>set -o pipefail; git fetch -q origin && git show origin/develop:macos_apps_mcp/eventkit.py | grep -c "def request_access_each"</automated>
    <fails_when>non-zero exit (file absent on develop), or the count prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The card-7 PR is `MERGED` via rebase; CI green; the review reported no open issue
    - `git worktree list` lists no `.worktrees/gate-card-7-runtime-split`
  </acceptance_criteria>
  <done>Card 7 is on develop; the device proof is queued for plan 01-08.</done>
</task>

</tasks>
<!-- plan-criteria-allow: R4 - the piped git verify starts with set -o pipefail, so the pipeline reports git's own exit status -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server process → EventKit/TCC | one EKEventStore on one worker thread; TCC grants attach to the responsible process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-15 | Denial of service | a second store or executor breaks EventKit thread affinity (hangs, TCC re-prompts) | medium | mitigate | eventkit creates no executor and dispatches through runtime.run_native; tests pin max_workers == 1 and no executor in eventkit |
| T-1-16 | Tampering | a moved body silently changes behaviour | medium | mitigate | byte-identity check against BASE for every moved name; device proof in plan 01-08 |
| T-1-17 | Elevation of privilege | TCC consent request moved and no longer fires from the daemon | medium | mitigate | request_access/request_access_each bodies byte-identical; doctor and server import them from eventkit; device proof `-k request_access` in plan 01-08 |
</threat_model>

<verification>
- `uv run pytest`, `uv run ruff check .`, `uv run ruff format --check .` pass on the merged head.
- runtime's public surface test and the executor test pass; byte-identity output recorded.
</verification>

<success_criteria>
GATE-02's code half is landed: runtime is the native door, EventKit is its own module, bodies are
unchanged. The device half follows in plan 01-08.
</success_criteria>

## Artifacts this phase produces

- `macos_apps_mcp/eventkit.py` (new module; the moved names listed in Task 1)
- `macos_apps_mcp/runtime.py::on_worker` (public, renamed from `_on_worker`); `::_classify_stuck_app`, `::_track_child` (renamed private)
- `tests/test_eventkit.py` (new); `tests/test_runtime.py::test_runtime_public_surface_is_the_native_door`; `tests/test_eventkit.py::test_eventkit_owns_no_executor`
- Branch `refactor/gate-card-7-runtime-split` (deleted after merge)

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-06-SUMMARY.md` when done.
</output>
