---
phase: 01-gate-land-the-spiked-architecture-cuts
plan: 14
type: execute
wave: 11
depends_on: ["01-13", "01-08", "01-07", "01-03"]
files_modified: []
autonomous: false
requirements: [GATE-13]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A daemon rebuilt from origin/develop after card 2 answers doctor() with version 0.11.0 and a build token that resolves to the develop commit it was built from, without -dirty (D-06)"
    - "One outbound check reports gated correctly after card 2 (send_mail dry-run preview when mail outbound is on; send_mail absent when it is off), and doctor's outbound report agrees with the tool list"
    - "The daemon's tool list shows dry_run default true for delete_event, delete_draft, delete_note and update_note"
    - "git branch --list 'spike/arch-review-*' prints nothing, and git worktree list shows no worktree on a spike/arch-review-* branch (D-09; GATE-13 empty edge)"
    - "origin/develop's history since v0.11.0 shows the sequential cards in the order 1 → 7 → 5 → 2 (GATE-13 ordering edge)"
  artifacts: []
  key_links:
    - from: ".worktrees/.daemon_probe.py"
      to: "the running daemon"
      via: "fastmcp Client over the daemon's unix socket"
      pattern: "_uds_client_factory"
  prohibitions:
    - statement: "MUST NOT delete a branch or worktree outside the spike/arch-review-* set, and never discard uncommitted work in a spike worktree without listing it first"
      status: resolved
      verification: test
---

<objective>
The last D-08 stop (D-08 stop 4, second half; D-06): rebuild the daemon from `develop` after card 2
and prove the gates, the dry-run defaults and the outbound ledger on the running daemon. Then D-09:
delete every `spike/arch-review-*` branch — including the non-landing 6, 6-baseline, 8 and 8-alt —
and remove their worktrees under `.claude/worktrees/` with `git worktree remove`.

Output: probe output, the landing-order record, the cleanup record — all in the SUMMARY. No release:
per D-06 the next release follows Phase 2 (GATE-12 device sweep green).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md
@.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-10-SUMMARY.md
@docs/DAEMON.md
@AGENTS.md

Known spike inventory at planning time (verify, do not trust): 11 local branches
`spike/arch-review-{1-native-seam,2-registration-record,3-fake-envelope-fixture,4-recoverable-preflight,5-tier-policy,6-baseline-13th,6-mailfilter,7-runtime-split,8-mailadapter-passthroughs,8-mailadapter-passthroughs-alt,9-script-preamble}`
and 9 worktrees under `.claude/worktrees/agent-*` checked out on them.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Owner go for the post-card-2 daemon swap and the spike cleanup (D-08 stop 4, D-09)</name>
  <decision>Rebuild and swap the daemon from develop now, then delete the spike branches and their worktrees.</decision>
  <context>
    The daemon swap drops connected MCP clients once (reconnect with `/mcp`). The cleanup deletes 11
    local branches and 9 worktrees under `.claude/worktrees/`; the spike commits stay reachable only
    through reflog afterwards. The executor lists any uncommitted changes in those worktrees before
    removing them and stops if there are any. Remote spike branches, if any exist on origin, are
    deleted too.
  </context>
  <options>
    <option id="swap-and-clean">
      <name>Swap the daemon and clean up now (recommended)</name>
      <pros>Closes the phase's device and hygiene steps in one session.</pros>
      <cons>Connected sessions drop once; spike branches are gone from the branch list.</cons>
    </option>
    <option id="swap-only">
      <name>Swap the daemon, keep the spikes for now</name>
      <pros>Spikes stay browsable.</pros>
      <cons>D-09 stays open; success criterion 6 is not met until it runs.</cons>
    </option>
  </options>
  <resume-signal>Type "swap-and-clean" or "swap-only"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Dev build from develop after card 2; probe version, build, outbound ledger, dry-run defaults and mail_index</name>
  <precondition>Task 1 answered "swap-and-clean" or "swap-only"; card 2 is merged on origin/develop; `security find-identity -v -p codesigning` lists "Developer ID Application: Andrei M. Lavrenov (VUMUR696L9)".</precondition>
  <files>(none in the repo — /Applications/macos-apps-mcp.app and the git-ignored .worktrees/.daemon_probe.py)</files>
  <read_first>
    - .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-10-SUMMARY.md (the probe and its pass criteria)
    - docs/DAEMON.md ("1. Build", "2. Move it", "The shim is per-connection")
    - macos_apps_mcp/registry.py (`outbound_status`) and macos_apps_mcp/doctor.py (deployment keys)
  </read_first>
  <action>
    Per D-06: same procedure as plan 01-10 Task 2 — `git worktree add --detach .worktrees/devbuild-card-2 origin/develop`,
    `BUILT=$(git -C .worktrees/devbuild-card-2 rev-parse HEAD)`, signed dev build
    (`scripts/build_app.sh --sign "Developer ID Application: Andrei M. Lavrenov (VUMUR696L9)" --out dist`),
    replace `/Applications/macos-apps-mcp.app`, `launchctl kickstart -k gui/$(id -u)/ren.lav.macos-apps-mcp`,
    bounded wait for the socket, run `.worktrees/.daemon_probe.py`. Extend the probe's output for this
    check: the `inputSchema` `dry_run` default of delete_event, delete_draft, delete_note and
    update_note from `list_tools` (all must be true), and `deployment.outbound` vs the presence of
    send_mail in `list_tools` (they must agree: mail registered ⇔ send_mail listed). Outbound check as
    in plan 01-10 (dry-run send to andrei@lav.ren when on; absence when off). mail_index surface
    ok/sidecar. Remove the build worktree. Tell the owner to reconnect clients.
  </action>
  <verify>
    <automated>uv run python .worktrees/.daemon_probe.py</automated>
    <fails_when>the probe cannot connect; or version is not 0.11.0; or the build token does not resolve to the built develop commit or contains -dirty; or any of the four dry_run defaults is not true; or outbound and the tool list disagree; or a send result without dry_run true; or mail_index is not ok/sidecar</fails_when>
    <human-check>Owner reconnects one Claude Code session; its doctor() shows the same build token, and calling delete_event with only an id returns a preview.</human-check>
  </verify>
  <acceptance_criteria>
    - Probe output pasted into the SUMMARY with `$BUILT`, meeting every criterion above
    - `git worktree list` lists no `.worktrees/devbuild-card-2`
  </acceptance_criteria>
  <done>The post-card-2 daemon runs develop's build with the record-driven gates and dry-run defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Record the landing order and delete every spike branch and its worktree (D-09)</name>
  <precondition>Task 1 answered "swap-and-clean".</precondition>
  <files>(none in the repo — local and remote branch refs, .claude/worktrees/ entries)</files>
  <read_first>
    - AGENTS.md ("Worktrees — one lane, always": `.claude/worktrees/` is harness-managed; a locked worktree you did not create belongs to another session)
    - .planning/phases/01-gate-land-the-spiked-architecture-cuts/01-CONTEXT.md (D-09)
  </read_first>
  <action>
    Landing order (GATE-13): `git fetch -q --prune origin`; `git log --oneline --reverse v0.11.0..origin/develop`
    and identify the merged commits of cards 1, 7, 5 and 2 (PR titles name the card); confirm the first
    commit of each appears in the order 1 → 7 → 5 → 2 and paste the list into the SUMMARY (cards 3, 4
    and 9 may interleave).

    Cleanup (D-09), each step listed before it runs: (1) `git branch --list 'spike/arch-review-*'` —
    the delete set; nothing outside this pattern is touched. (2) `git worktree list --porcelain` — for
    every worktree whose `branch` is `refs/heads/spike/arch-review-*`: run
    `git -C <path> status --porcelain`; if any prints changes, STOP and report them (never discard
    uncommitted work); else `git worktree unlock <path>` if locked by this owner's host, then
    `git worktree remove <path>`. (3) `git branch -D` each branch from step 1. (4)
    `git ls-remote --heads origin 'spike/arch-review-*'` — for each remote head found,
    `git push origin --delete <branch>`. (5) `git worktree prune`. Journal the phase close
    (`vault-journal`: "gate landed — cards 1/7/5/2 + 3/4/9; spikes deleted").
  </action>
  <verify>
    <automated>wt="$(git worktree list --porcelain)" && test -z "$(git branch --list 'spike/arch-review-*')" && test -z "$(git ls-remote --heads origin 'spike/arch-review-*')" && ! printf '%s' "$wt" | grep -q 'refs/heads/spike/arch-review-'</automated>
    <fails_when>non-zero exit (a spike branch remains locally or on origin, or a worktree is still checked out on one)</fails_when>
  </verify>
  <acceptance_criteria>
    - The landing-order list in the SUMMARY shows card 1 before card 7 before card 5 before card 2
    - The SUMMARY lists every deleted branch (11 expected) and every removed worktree (9 expected), and states that none had uncommitted changes
    - `git branch --list 'spike/arch-review-*'` prints nothing; no worktree remains on a spike branch
  </acceptance_criteria>
  <done>Success criterion 6 is met: cards landed in order, daemon checks done, spike branches and their worktrees removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → launchd daemon | the installed bundle serves every MCP client with the owner's TCC grants |
| cleanup → local and remote git refs | deleting refs is irreversible outside the reflog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-41 | Elevation of privilege | after card 2 the daemon registers a send or write tool it should not | high | mitigate | probe checks outbound ledger ⇔ tool list agreement and dry-run-only sends |
| T-1-42 | Tampering | cleanup deletes a non-spike branch or another session's worktree | medium | mitigate | delete set built only from the `spike/arch-review-*` pattern; worktrees chosen by their checked-out branch; each list printed before action |
| T-1-43 | Tampering | uncommitted work in a spike worktree is destroyed | medium | mitigate | `git status --porcelain` per worktree; any change stops the cleanup and is reported |
| T-1-44 | Tampering | a dirty or wrong tree is installed as the daemon | medium | mitigate | detached worktree at origin/develop; build token must equal `$BUILT` without `-dirty` |
</threat_model>

<verification>
- Probe meets every criterion; landing order recorded; spike refs and worktrees gone.
</verification>

<success_criteria>
Success criterion 6 (D-06 reading) and D-09 are complete; the gate phase is ready for verification.
</success_criteria>

## Artifacts this phase produces

- No new code symbols. `.worktrees/.daemon_probe.py` extended locally (git-ignored).

<output>
Create `.planning/phases/01-gate-land-the-spiked-architecture-cuts/01-14-SUMMARY.md` when done.
</output>
